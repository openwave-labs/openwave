# M5.32 candidate ledger: the three Lagrangian searches, one table

> Rung R13-L of [M5.32](../tasks/m5_32_task_details.md). Compiled 2026-09-02 from three pinned records. Every row cites a script, a receipt, or a commit-pinned document; nothing in the status column is asserted from memory. The model author asked for exactly this (2026-09-02): which candidates from the three searches are the most promising, and what the repair options are for the two open issues.

| Source | Pinned at | Record |
| --- | --- | --- |
| OpenWave M5.32 (this repo) | `main` `f67bd7d0` (2026-09-02) | [`m5_32_task_details.md`](../tasks/m5_32_task_details.md) § RUNG LOG, [`m5_32_ledger.json`](../data/m5_32_ledger.json), [`m5_32_method_note.md`](m5_32_method_note.md) |
| Vantasner substrate-framework (P239 to P249) | `8b74d3ab` (2026-09-02) | [`vantasnerdan/substrate-framework`](https://github.com/vantasnerdan/substrate-framework/tree/8b74d3ab) |
| Mikulski the-final-lagrangian-of-physics (reports 001 to 013) | `65c6177f` (2026-08-31) | [`mjmikulski/the-final-lagrangian-of-physics`](https://github.com/mjmikulski/the-final-lagrangian-of-physics/tree/65c6177f) |

Result vocabulary (adopted from the parallel program's 2026-08-31 review, used in every status cell): `ESTABLISHED_KINEMATIC` (a finite quantity on a chosen family, no dynamics), `ESTABLISHED_SYMMETRY` (an exact group action with a conserved charge), `MODE_EXISTS` (a localized linear mode on a converged background), `PERIODIC_ORBIT_EXISTS` (a nonlinear relative equilibrium), `CANDIDATE_REFUTED` (a pre-registered gate failed on that candidate), `NUMERICALLY_UNRESOLVED` (the instrument could not decide). A refutation refutes the candidate as run, never the question.

## 1. The problem, stated once

### 1.1 The field and the certified 3+1 action (OpenWave, the model author's M5 with the 4x4 promotion)

```text
M(x)          real symmetric 4x4, vacuum M_vac = diag(-g, 1, delta, 0) (s = -1 on the time row), g = 8 or 32, delta = 0.3
A_mu          = d_mu M                                    (the jets)
F_mu nu       = A_mu eta A_nu - A_nu eta A_mu             (curvature, quadratic in the jets)
<F, G>_eta    = tr(eta F eta G^T)
I1            = sum_{mu<nu} eta^mumu eta^nunu <F_mu nu, F_mu nu>_eta = (1/2) F_abcd F^abcd
V4            = w sum_{p=1..4} (tr((M eta)^p) - C_p)^2,   C_p = (s g)^p + 1 + delta^p,  w = 7.24023879e-4
L_cert        = -4 I1 - V4                                (CERTIFIED_COEFFS)
```

### 1.2 The two open issues, as gates

```text
NEWTON SIGN     two defects at separation d: E_int(d) = E(pair) - 2 E(single).  Physics wants dE_int/dd > 0
                (attraction).  Under L_cert the boost-dressed pair is REPULSIVE on every instrument run (R0, R3, R11).
OMEGA DIVERGENCE  the clock is the isorotation M(t) = R(omega t) M R(omega t)^T in the (2,3) eigenplane, tangent a0.
                Fixed charge J = 2 kin omega, energy E_J = E_stat + J^2/(4 kin).  With the RIGID isorotation the
                inertia kin = -4 x (omega^2 coefficient of I1) grows linearly with the box (kin ~ kappa L, R4, R7, R10,
                R12), so omega* = J/(2 kin) ~ 1/L: the frequency is a box label, not a property of the object.
```

The parallel program's 2026-08-31 group fact sharpens the second issue: the continuous stabilizer of `diag(-g, 1, delta, 0)` in SO(1,3) is trivial (the discrete part is the Klein four-group, the OpenWave R9 receipt), so NO exact S^1 fixes the certified vacuum. Any clock on this vacuum is either rigid (extensive inertia) or weighted by hand (kinematic only, `w -> c w` rescales `omega*` freely). A localized clock therefore needs one of: a degenerate eigenvalue pair somewhere (a vacuum with `d2 = d3`, or a wall where it holds), an added field whose orbit norm vanishes in the vacuum, or a different action.

### 1.3 Equation-to-code map (OpenWave)

| Equation | Code |
| --- | --- |
| `I1`, `V4`, the term registry, the Legendre pairs | [`m5_32_lagrangian.py`](../scripts/m5_32_lagrangian.py) `I1_sym`, `V4_sym`, `term_lagrangian`, `term_hamiltonian`, `certified_energy` |
| `a0`, `kin`, the FIRE relaxation (no Hamiltonian time integrator exists in this stack; `v += dt F; M += dt v` is damped descent) | [`m5_21_3_a_4d.py`](../scripts/m5_21_3_a_4d.py) `gen_catalog`, `kin_of`, `fire` |
| Newton instruments (ansatz at finite `m`, relaxed pairs, cross-inertia) | [`m5_32_r3_i_ansatz.py`](../scripts/m5_32_r3_i_ansatz.py), [`m5_32_r3_ii_pair.py`](../scripts/m5_32_r3_ii_pair.py), [`m5_32_r3_iii_crossinertia.py`](../scripts/m5_32_r3_iii_crossinertia.py), [`m5_32_r11_a_samesign.py`](../scripts/m5_32_r11_a_samesign.py) |
| Clock instruments (fixed-J ladder, box ladder, relaxed ring) | [`m5_32_r4_clock.py`](../scripts/m5_32_r4_clock.py), [`m5_32_r10_relaxed_ladder.py`](../scripts/m5_32_r10_relaxed_ladder.py), [`m5_32_r12_a_ring.py`](../scripts/m5_32_r12_a_ring.py) |
| P249 exact algebra, re-derived independently | [`m5_32_p249_check.py`](../scripts/m5_32_p249_check.py) (7/7) |

## 2. OpenWave M5.32, R0 to R12 (every rung independently audited; verdicts from [`m5_32_ledger.json`](../data/m5_32_ledger.json))

| Candidate | Action / term | Issue | Claim as run | Status | Evidence | What would move it |
| --- | --- | --- | --- | --- | --- | --- |
| `L_cert = -4 I1 - V4` (control) | certified base | both | stack reproduces to 2e-15; the author's 2026-08-17 notebook reproduces (A 863.733, B 167.668, sign +); far field is `(32 pi/3)(ln d)/d + beta/d`, repulsive at every visible order | control row, Newton REPULSIVE | R0 ([`m5_32_r0_audit.json`](../data/m5_32_r0_audit.json)) | nothing: it is the reference |
| C0/C1: `I2..I6` mixed contractions + `E1..E3` parity-odd | constant-coefficient current-order insertions | Newton sign | can any constant coefficient reverse the boost weight while keeping vacuum stability? | `CANDIDATE_REFUTED` (form-level Farkas / LP certificates at three `(g, delta)`, every channel, with or without epsilon terms; every lattice reversal window unbounded below, converged n = 64/96/128) | R1 ([`m5_32_r1_audit_symbolic.json`](../data/m5_32_r1_audit_symbolic.json), [`m5_32_r1_audit_screen.json`](../data/m5_32_r1_audit_screen.json)) | a non-constant coefficient (field-dependent) or a higher-order term; not more of the same class |
| C2: `L_lambda = -4[(1-lambda) I1 + lambda I1_h] - V4`, `h = eta + 2 u u` | covariant metric mixing, boost weight `1 - 2 lambda` | both | bounded by theorem for `lambda >= 1/2`, Coulomb kept, static sector = `I1` | Newton: `CANDIDATE_REFUTED` (repulsive on all three instruments, every lambda); clock: `CANDIDATE_REFUTED` (no interior fixed-J minimum, `R* = L/2`, `omega* L = 7.1` constant, static gain unbounded in R) | R2, R3, R4 | nothing at this order; kept as the audited C2 control |
| C3: potential-only localizers | any static potential deformation | omega divergence | could a potential term localize the clock? | `CANDIDATE_REFUTED` by theorem (orbit blindness: the clock orbit leaves every potential invariant; Derrick: only a positive 2-derivative term localizes boundedly) | R6 ([`m5_32_r6_audit.json`](../data/m5_32_r6_audit.json)) | none: class killed |
| C4: `K_T` time-row gradient penalty | `L_lambda - c2 K_T` | omega divergence | penalize wide boost dressings, Derrick `E ~ mu` | `CANDIDATE_REFUTED` for G3 (13/13 audited, 0 refuted): interior window exists in `c2` but the clock inertia stays IR-extensive (B7) | R7 ([`m5_32_r7_audit.json`](../data/m5_32_r7_audit.json)) | none on the rigid clock; the obstruction was later traced to the clock convention, not to the term |
| C5: `(F.F)^2 = Q_I1sq` (the author's `(I1)^2` proposal) and C6 non-commutator quartics | quartic in the jets | omega divergence | a quartic inertia is IR-convergent, so a fixed coefficient could hold `omega*` | C6: `CANDIDATE_REFUTED` (volume-extensive, vacuous); C5: `NUMERICALLY_UNRESOLVED` on the ansatz (no continuum limit, R8, R9), then `CANDIDATE_REFUTED` at fixed coefficient (42 % drift, static energy driven negative, R11) | R8, R9, R11 ([`m5_32_r11_audit.json`](../data/m5_32_r11_audit.json)) | the direct dilation test against a flipped static sector (owed, PAUSE RECORD 3) |
| The point hedgehog (the object) | `L_cert`, `m = 0` ansatz | both (the source for Newton, the carrier for the clock) | a protected degree-1 soliton | `CANDIDATE_REFUTED` as a protected object: the degree instrument reads one eigenvector (`pi_2(OPS) = 0`, `pi_1 = Q8`), no barrier from the unrelaxed ansatz; the relaxed core is physical (radius 3.6 to 4.0) but its inertia is 78 % boundary | R9, R10 ([`m5_32_r10_audit.json`](../data/m5_32_r10_audit.json)) | nothing: use a protected object |
| The charged ring (2+2 half-vortex, the author's endorsed object) | `L_cert`, M5.21.2 seed | both | protected `q = 1/2`; shrinks 6.5 % sub-grid decelerating (park vs collapse open); rigid clock inertia extensive with the SAME shell shape as the hedgehog | object: `ESTABLISHED_KINEMATIC` (protected, metastable); clock: `CANDIDATE_REFUTED` on the rigid convention; tapered: `ESTABLISHED_KINEMATIC` only (the `w -> c w` objection) | R12 ([`m5_32_r12_audit.json`](../data/m5_32_r12_audit.json)); the 0.786 radiation gate was cross-model and is withdrawn ([CORRECTION](../tasks/m5_32_task_details.md)) | the 12000-iteration ladder (park or slide) and a relaxed-ring fixed J with a box-scaled taper (owed) |
| The author's 2026-08-29 global sign flip (`Hs -> -Hs`) | `L_cert` with the static sector negated | Newton sign | reverses the pair sign | `CANDIDATE_REFUTED`: the negated static sector has NO floor on exact Lorentz orbits (dilation identity), and the same-sign pair stays repulsive under `L_cert` | R11 | none: a global flip cannot be local |
| Weighted / tapered clock flows (the old R13 list: spectrum-departure weight, core-restricted isorotation) | `L_cert`, `a0 -> w(M) a0` | omega divergence | finite inertia by construction | `ESTABLISHED_KINEMATIC` at best, never run as a fixed-J clock: `w -> c w` gives `C -> c^2 C`, `omega* -> omega*/c`; no compact period fixes the normalization | the parallel program's 2026-08-31 objection, accepted on 2026-08-31 | a compact group action with a fixed period (R13A), or a wall / degeneracy that makes the rigid flow invisible in the vacuum (R13-W) |

**Net OpenWave position.** No constant-coefficient term at current order fixes either issue on the certified vacuum. The one obstruction every clock rung ran into is the rigid isorotation of the vacuum frame far from the defect, which is a property of the clock convention (Q60), not of the object or of any term. The Newton sign has never been reversed on this action by any local mechanism.

## 3. Vantasner substrate-framework, P239 to P249

Different action from § 2 in every row: the P239/P240 base is `L = -1/2 F F eta eta h h - V_M5.17` with the internal metric `h = eta - 2 P_t eta` (`P_t` the simple timelike spectral projector of `eta^-1 M`), plus a projector current, a Landau-de Gennes lift with exterior spectrum `(g, 1, 0, 0)`, and later an auxiliary clock axis with locks. So its exterior is ALREADY degenerate in the tangent pair (two zero eigenvalues), unlike our `(-g, 1, delta, 0)`. P249 is a third action (Sym(3) + complex scalar + locks). Records split across `proposals/` and `campaigns/`; 30 accepted claims in this range. Digest with verbatim quotes and the claim boundaries C-M5C-001 to 004: `scratchpad/r13l/vantasner_digest.md` (local, not tracked). None of the P239 to P248 numbers was re-derived by us in this rung (§ 7).

| Candidate | Action / term | Issue | Claim as run | Status (their word, then the review vocabulary) | Evidence | What would move it |
| --- | --- | --- | --- | --- | --- | --- |
| P239 candidates A to K, the base action + projector current + LdG lift + dilaton + guard + axis lock | spectral-Cartan 4x4 action (above) | all three | candidate H (timelike dilaton `A(tau) = exp(2 alpha (tau - g))`, `L_tau = -(kappa_tau/2)(d tau)^2`) gives an exact linearized attractive exchange `U_N = -alpha^2 m1 m2/(pi kappa_tau r)`; no one-body branch ever passed the frozen gates (attempt 0014 gradient 1.95e-3 vs 5e-4) | `active`, "no frequency, pair, registry, or release claim is accepted"; Newton: an action-level theorem for an ADDED scalar mediator, `ESTABLISHED_SYMMETRY`-grade algebra, no relaxed pair; clock: `NUMERICALLY_UNRESOLVED` | [`proposal.yaml`](https://github.com/vantasnerdan/substrate-framework/blob/8b74d3ab5fdd307ba564291ff7bfa702db5d3330/proposals/P239-m5-4x4-action/proposal.yaml) | a stationary one-body branch; a relaxed two-defect solve with the dilaton on |
| P240 spectral-Cartan fixed-J two-clock candidate | same base; two-clock sector `E_J = E_stat + (1/4) J^T I^-1 J` | omega (one body), Newton (cross inertia) | covariance, positive Hamiltonian, exact 3x3 + Coulomb recovery, affine-clock sign repaired; the only one-body fixed-J branch (6x5, `I = 0.4607`, `omega = 1.0853`) is a SADDLE (`lambda_min = -2.868`); attraction at fixed J iff the cross inertia `C(r) = A/r` has `A > 0`, never measured; fixed omega gives the OPPOSITE sign (`+2 C omega^2`) | `active`, `candidate_selected_independent_validation_requested`; `not_established`: stable hedgehog, relaxed pair, positive `A/r`, attractive tail; clock: `NUMERICALLY_UNRESOLVED` (saddle); Newton: conditional, open | [`current-candidate-receipt.yaml`](https://github.com/vantasnerdan/substrate-framework/blob/8b74d3ab5fdd307ba564291ff7bfa702db5d3330/proposals/P240-m5-kinetic-axis/evidence/current-candidate-receipt.yaml) | a measured `C(r)` between two relaxed clocks (our R3.iii found it undecidable unrelaxed, the same wall) |
| Confined window clock (P240 attempts 0041 to 0043, consumed by P243, P244, P246) | same base, structural wall pin at the rank-1 projector, R = 12 order 16 | omega | `omega = 0.790394`, `I = 0.632596`; all 32 kinetically non-singular modes certified (`omega_min = 1.046`); `omega_min` RISES with the box (1.010 / 1.046 / 1.089 at R = 10 / 12 / 14) | accepted in the "confined (window-assisted) class" only; by the repo's own rule (imposed support cannot establish natural localization) it is `ESTABLISHED_KINEMATIC` | C-M5S-006, 009, 010, 013 | de-boxing (P247, below) |
| P247 de-boxed isolated clock + boost-alignment mass | same base; wall removed; `s_m = (m^2/2)((u^T eta xi)^2 - 1)` | omega and isolation | fixed-J energy grows without plateau on both ladder segments (order 16: 55.1042 at R = 12 to 59.2462 at R = 18; order 24: 58.7379 at R = 20 to 62.5617 at R = 30; the two segments are non-monotone at the junction); width is a quasi-modulus; the R = 28 stationary point is a Morse-14 saddle; the Yukawa boost route dies by Z2 evenness at machine precision on the measured backgrounds (C-M5S-016, no all-background parity proof) | C-M5S-015, verbatim: "no tested construction on the radial pinned-wall ladders supports a de-boxed isolated fixed-J clock"; deeper-box existence "open, not denied": `CANDIDATE_REFUTED` on the tested ladders only | C-M5S-015 to 020 | a non-spherical or deeper-box member; or the P249 route |
| P243 induced-gravity coupling | the accepted Sakharov-type composition `1/G_total = B + N(1 - 6 xi) Lambda^2 J/(12 pi)` applied to the clock's N = 3 massless species | Newton sign (as gravity) | `G_total = 4 pi/Lambda^2 = 46.807` at B = 0, xi = 0, ATTRACTIVE; but `epsilon = G_total M/R = 213.4 >> 1`, the weak-field regime is invalid; the confined fixed-J two-clock pair of the spectral-Cartan action (P240 attempt 0043, wall-pinned clocks, summed-angle winding channel) is REPULSIVE, `E_int = +456.6 d^-1.696` (C-M5S-008); it is not a boost-dressed pair and not a pair on `L_cert` | accepted, "applicability-bounded"; not a repair of the M5 sign: an imported mediator out of its regime | C-M5S-003, 004, 007, 008 | nothing within induced gravity: the dichotomy (strongly coupled induced, or bare-dominated weak field) is exact |
| P245 / P246 nonlinear self-gravity of the confined clock | Einstein constraint in areal gauge on the frozen source; Painleve-Gullstrand completion | gravity | the frozen source is TRAPPED: max compactness `2Gm/r = 893.8` (horizon at 1), pure dilation bottoms at 881.5; the classical source admits a regular horizon-penetrating completion; the clock is axisymmetric, not spherical (7.2 % pressure anisotropy) | `completed`; established as a classification: "not a regular horizonless static object at the purely induced coupling" | C-M5S-012, 013, 014 | a coupled stationary axisymmetric Einstein-M5 solve (their open frontier) |
| P249 exterior-degenerate SO(2) clock | NEW action: `S` in Sym(3) + complex `psi`, unit axis lock, phase lock `6 norm(B - Q(psi))^2`, `W(f) = 3f^2 - 4f^3 + 2f^4`, canonical Cartan kinetics | omega (one clock) | exact compact SO(2) fixing the exterior `(N N^T, 0)` pointwise; Hessian `diag(5,10,22,4,4,22)`, mass squares `(10,10,22,4,4,22)`, scalar 6; charged edge `min(6, 4, 22/4) = 4`; split plateau `2V/I = 45/16`, margin 19/16; Benci-Fortunato Theorem 18 gives a stable minimizing family with `0 < omega^2 < 4` and a box-independent charge radius; zero production numerics | `accepted`, `CAMPAIGN_SUCCESS`; in the review vocabulary `ESTABLISHED_SYMMETRY` + `PERIODIC_ORBIT_EXISTS` at theorem level; the exact algebra CONFIRMED 7/7 by [`m5_32_p249_check.py`](../scripts/m5_32_p249_check.py); the variational bridge NOT checked by us | C-M5C-001 to 004 (boundaries quoted verbatim in the digest): no particle, two-clock, force, de Broglie, or gravity identification; thresholds belong to this completion only | a numerical profile (optional, their words); the finite-shell bubble variant we asked for on the thread; any field map from this action back to the certified 4x4 field |
| P241, P242, P248 | external paper audits (Fulceri continuum, Euler elastodynamics, the Sciama gothic metric) | other | not M5 actions; P248's sign ledger concerns that paper's Newtonian field energy, no M5 dependency | orthogonal to this ledger | | |

**Net Vantasner position.** "no campaign has an accepted attractive Newton tail between separately relaxed M5 defects": the attractive statements are an added dilaton's linearized exchange (P239 H), a conditional on an unmeasured cross inertia (P240 D), an imported induced coupling out of regime (P243), or a frozen telescope (P247). The clock line ends in P249, on a new action. No campaign uses `omega(x)`, walls, or frequency interfaces (one grep hit, an acoustic interface in a paper review); the repo's stated rule is only against IMPOSED support (taper, mask, pinned wall, cutoff, box) as a localization device. Vocabulary note: the tokens `ESTABLISHED_KINEMATIC`, `PERIODIC_ORBIT_EXISTS`, `CANDIDATE_REFUTED` come from the 2026-08-31 review comment and do not occur in the repo at the pin; "rung packet" is not a repo term. The repo's unit is an obligation node (`requires`, `pass_licenses`, `does_not_license`, `maximum_verdict`, `failure_scope`, `unlocks`) with a typed license chain (object, symmetry or conservation, ensemble, variational functional, admissible space, representation coverage, observable, numerical representation, permitted verdict) and, since PR #192, an analytic-closure receipt before any production numerics.

## 4. Mikulski the-final-lagrangian-of-physics, reports 001 to 013

Same field, same `F`, same vacuum signature (`diag(-g, 1, delta, 0)`, g = 8), so these rows are directly comparable to § 2. Convention: the reports' `I_1 = F_{mnab} F^{mnab}` is exactly twice our `I1` (audit script D.4), so their `-I_1 = -4 omega^2` on the clock witness reads `-4 I1 = -8 omega^2` in our units; every sign statement survives. Their "3x3-preserving family" means the static 3x3 sector (time row of `M` constant). Stack: torch autograd, 32^3 box at h = 1.5 (L = 48), pinned vacuum shell, the working metric `G = eta + 2 u u` on the matrix slots (our `h` of the C2 family). Reproduced here: 001 (2 s, every quoted number matches), 007 (the self-contained legs and the numpy route 2 on the committed fields, 1.8e-15; the lattice producers need report 004's GPU fields and did not run here); 006 (744 s CPU, both routes pass, every README number matches: chain-of-7 ratio 1.8037, IR slope 85.35, `X = +11.630`, gaussian pocket -0.8204; the `I4/I1 = 0.763` lattice cost is an external artifact needing the 004 fields). Digest with the verbatim headline sentences: `scratchpad/r13l/mikulski_digest.md` (local, not tracked).

| Candidate | Action / term | Issue | Claim as run | Status | Evidence | What would move it |
| --- | --- | --- | --- | --- | --- | --- |
| Quadratic contractions `-I_1 + a N_1 + b N_2 + c N_3` (the 3x3-preserving family) + the epsilon sector | constant-coefficient quadratics, parity-even and odd | clock sign channel; Newton by intent | "No choice of (a, b, c) can repair the negative clock channel while retaining the full 3x3 action"; the odd sector "closes" | `CANDIDATE_REFUTED` (same content as our R1, third independent implementation; 005 corrects P239's four pseudoscalars to three on realizable fields, `P_dd = 0`) | [001](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/001-quadratic-contractions/README.md), [005](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/005-epsilon-contractions/README.md); reproduced here | none: class closed on three stacks |
| Boost-hedgehog Newton no-go | any constant-coefficient quadratic on the author's boost-dressed ansatz (first order in `m`) | Newton sign | `F` is purely spatial on the ansatz, so the repair space vanishes identically; every quadratic collapses to `alpha e_1 + beta e_4` with the virial `3 S_1 = 4 S_4`; measured `t_1 = E1_int/E4_int` in [1.335, 1.380] on the clean rows (to 1.427 on the UV-cutoff-flagged `p = 0.75` row) and `X = 3 E1_int - 4 E4_int > 0` close every sign branch; the author's flipped baseline attracts but is negative-definite with IR-divergent notebook numbers (`S_1 ~ 85.3 R`) | `CANDIDATE_REFUTED` for the whole quadratic class at leading order (our R0.10 log-enhanced far field and R11 no-floor result, seen from the ansatz side) | [006](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/006-newton-boost-hedgehogs/README.md) + the flat-connection appendix (the dressing is pure gauge) | higher order in `F`, `dM`-built coefficients, O(m^6) dressings, or non-pure-gauge configurations (the author's own escape map, no public report yet) |
| Field-dependent coefficients `c(M)` and pointwise connections `C_mu(M)` | coefficients from algebraic invariants of `M`; so(1,3)-valued connections built from `M` | Newton sign | `c(M)` is exactly constant along boost dressings (eta-similarity), so blind to Newton at leading order; no nonzero `C_mu(M)` exists pointwise; the index-mixed escape fills the vacuum with `F_vac = lambda^2` | `CANDIDATE_REFUTED` (exact) | [007 § 1 + appendix](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/007-core-weighted-clock/APPENDIX-no-connection-from-M.md) | none pointwise |
| C3 clock `H = -a B_k + 3 b B_k^2` (u-selected boost quartic), local density | `L_C = rot^2 - a boost^2 + b (boost^2)^2` with the G split | omega divergence | finite `omega*^2 = a/(6 b k_2)` pointwise (measured 0.913); on the lattice the local quartic DELOCALIZES (PR 88 to 1962, no interior minimum through omega = 2.8) | pointwise: `ESTABLISHED_KINEMATIC`; lattice: `CANDIDATE_REFUTED` (dilution: drive linear in density, brake quadratic) | [002](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/002-covariant-split-and-clock/README.md), [003](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/003-canonical-analysis/README.md), [004](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/004-lattice-clock/README.md) | a convex template or a nonlocal brake (the next two rows) |
| Core-weighted intensive quartic `E_c = -a B + 3 b B^2`, `B = int c_w b_k`, `c_w = v_4/(v_4 + v_0)` | nonlocal (square of an integral), core weight from the potential density | omega divergence | interior well at the omega = 0.8 rung, ~100 core sites, on fresh frozen-mask and dynamic-weight ladders | `ESTABLISHED_KINEMATIC` on one box (energy reading; the author calls the form "nonlocal", admissibility "author-gated"; box independence NOT measured; the frozen tangent is not translation-covariant) | [007](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/007-core-weighted-clock/README.md); route 2 reproduced here | an L-ladder at fixed h; a compact period fixing the weight normalization (the `w -> c w` objection applies verbatim) |
| `gamma (I_1^G)^2`, gamma = 70.61 (the author's article candidate in the repaired metric) | local quartic, G on the matrix slots | omega divergence | raw-eta form is inert; the G form has a negative time part and, in the ENERGY-FUNCTIONAL reading, an interior well at omega = 0.35 (frozen prediction 0.326, PR ~100, depth 6.5e-5); the fundamental (Legendre) reading carries `-gamma s^2` and runs away (energy to -7e13); ordering `E(0.2) > E(0.35) < E(0.5)` holds through 24 extra L-BFGS cycles | energy reading: `ESTABLISHED_KINEMATIC` on one box, convergence-certified in depth but not in L; fundamental reading: `CANDIDATE_REFUTED` (runaway) | [008](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/008-i1-squared-clock/README.md), [013](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/013-window-convergence/README.md), [012](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/012-delta-g-scaling/README.md) (well exists at g = 8, 64, 512, position drifting 1.0x to 3.0x the prediction, PR 1165 in that protocol) | the model owner's ruling on whether the energy functional is the `H` of a well-posed `L` (the author's own open question); an L-ladder |
| Fundamental-reading grid `L = -I_1/2 - V + gamma (I_j)^2` over the u-decorated family (C10, C13, C16, C19) | 18-dimensional family closed exactly; grid Legendre theorem `H = alpha(-s + k) + gamma(-s^2 + m^2 + 2 s k + 4 m k + 3 k^2) + V` | omega divergence in the canonical reading | inside a two-sided gamma window (x10 to x20) fixed-depth interior wells at omega* 0.1 to 0.2; 24-cycle continuation dismantles the x14 bracket and erodes x10, no plateau | `NUMERICALLY_UNRESOLVED` leaning refuted ("budget-bounded negative"; "the energy-functional reading remains the only reading with a convergence-certified localized clock") | [010](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/010-fundamental-grid-clock/README.md), [013](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/013-window-convergence/README.md) | a same-channel drive-brake pairing outside the single-invariant grammar (the author's "missing convex template") |
| Rotational (spin) clock: rigid isorotation, equivariant textures, core-breaking texture | frozen rotation tangent `[W, M]`, constrained fixed-J surrogate | the extensive-inertia issue, spin channel | pure rotation inertia extensive (`I ~ L^2.93` at L = 24, 36, 48); equivariant textures have a trivial channel; only a hand-built core-breaking texture has finite inertia (`I ~ L^0.14`), not selected by statics; prescribed J buys peripheral inertia (r ~ 15 of 18), not core spin; axial line tension constant (7.7 to 8.3)e-4 | `CANDIDATE_REFUTED` as a core spin mode (the author's "spin question fully open"); the finite-inertia texture is `ESTABLISHED_KINEMATIC` and the ONLY box-independent inertia in the three searches | [009](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/009-rotational-clock/README.md), [011](https://github.com/mjmikulski/the-final-lagrangian-of-physics/blob/65c6177f6255aaa4bed2779aa8866022ba78ac3b/reports/011-third-class-rotor/README.md) | a mechanism that makes the global rotation act on the core only, selected by the energy (this is the wall question, from the spin side) |

**Net Mikulski position (the author's own words, 013):** "the energy-functional reading remains the only reading with a convergence-certified localized clock". No box ladder was ever run for a boost clock; the Newton line is a chain of closures identical in content to ours; no wall, interface, or `omega(x)` construct exists in the repo (grep over all markdown at the pin). One structural fact stated there and true of our stack too: `F_0i = [dM/dt, d_i M]_eta` vanishes wherever `d_i M = 0`, so the VACUUM carries no kinetic energy under any clock rotation; the extensive inertia is the defect's orientation tail (`1/r`) times a non-decaying generator, on both stacks.

## 5. The two issues, all repair routes across the three searches

The author's two issues, every route the three searches tried, one status each. "Closed" means a pre-registered gate failed or an exact no-go exists on that route; "open" means untested or undecided. Where two stacks reached the same closure independently, both are cited.

### 5.1 Newton sign (the boost-dressed pair must attract; it repels under `L_cert`)

| Route | Tried by | Outcome | Open? |
| --- | --- | --- | --- |
| Constant-coefficient current-order quadratics, parity-even (`I2..I6`, `N1..N3`) | OpenWave R1 (Farkas certificates); Mikulski 001, 006 (the repair space vanishes identically on the boost ansatz); Vantasner P240 0032 (rank 6 + 4, witness `-4 omega^2`) | ❌ closed on three stacks: cannot reverse the boost weight and keep the vacuum | no |
| Parity-odd (epsilon) quadratics | OpenWave R1 (all vanish on the static sector); Mikulski 005 (three pseudoscalars on realizable fields, `P_dd = 0`) | ❌ closed | no |
| Global sign flip of the static sector (`Hs -> -Hs`, the author's 2026-08-29 notebook) | OpenWave R11 (no floor on exact Lorentz orbits); Mikulski 006 (the flipped baseline attracts but is negative-definite, `S_1 ~ 85.3 R`) | ❌ closed: attraction bought with an unbounded energy | no |
| Field-dependent coefficients `c(M)` from algebraic invariants; pointwise connections `C_mu(M)` | Mikulski 007 + appendix (exactly constant along boost dressings; no so(1,3) connection from `M` alone) | ❌ closed at leading order (exact) | no |
| Covariant metric mixing `h = eta + 2uu` (our C2 `I1_h`, their spectral-Cartan `h`) | OpenWave R2, R3 (repulsive at every lambda on three instruments); Vantasner P240 (conditional on an unmeasured `A > 0` cross inertia) | ❌ closed on our instruments; conditional on theirs | only through a measured relaxed cross inertia `C(r)` |
| Fixed-J two-clock cross inertia (attraction iff `C(r) = A/r`, `A > 0`) | Vantasner P240 0034 (exact algebra); OpenWave R3.iii (undecidable unrelaxed: sign is a window and amplitude choice) | 🔶 the algebra is exact; the number was never measured on relaxed clocks by anyone | YES: needs two relaxed, localized clocks first (so it waits on the omega issue) |
| Added scalar mediator (P239 candidate H, timelike dilaton) | Vantasner P239 0007 (linearized exchange attractive by construction) | 🔶 an action-level theorem for a new field; no stationary one-body branch, no relaxed pair | open, but it is a new field, not a repair of the 4x4 action |
| Induced gravity (Sakharov composition on the clock's massless species) | Vantasner P243, P245 | ❌ closed as a weak-field mechanism: `G M/R = 213`, the source is trapped (compactness 894 over the horizon) | no (exact dichotomy) |
| Curvature-quadratic contractions `R_ac R^ac`, `R^2` (the author's 2026-08-20 public quest, Q49) | OpenWave R0.3 / R1 (one independent mixed trace; `R` not symmetric; inside the constant-coefficient class) | ❌ absorbed by the R1 closure at current order | no at constant coefficient |
| Higher order in `F`, `dM`-built coefficients, O(m^6) dressings, non-pure-gauge configurations | Mikulski 006 § 7 escape map (no public report); OpenWave R11 U2 named the one candidate in this class with a cheap direct test: `(F.F)^2` scales as `s^5` under dilation, so it is the one term that could restore a floor to a flipped static sector (the owed dilation test, PAUSE RECORD 3) | 🔶 untested for the sign; the OpenWave term is named, the test not yet run | YES (the only class on the 4x4 field with a named term and a direct test) |
| Yukawa boost exchange between clocks (P247 route F1, the candidate-B boost-alignment mass) | Vantasner C-M5S-016 / 017 | ❌ closed: no odd-order classical static boost sourcing (Z2 evenness at machine precision on the measured backgrounds); the mass "weighs a channel that carries no odd-order classical static source" | no |
| Frozen-superposition potential telescope, `Delta E(d) = 8.1133 - 5.668 exp(-0.2442 d)` | Vantasner C-M5S-020 | 🔶 attractive over d = 6 to 28 but "not the relaxed two-clock interaction law" and not a Yukawa mass | only as a hint for a relaxed pair |
| Literal negative-square contraction metric `R^2 - (g^2 R~)^2` | Mikulski 002 § 3 | ❌ closed: worsens the instability as `g^4` | no |
| Short-range gaussian pocket (`E1_int(1.0) = -0.82`, repulsive again at d = 0.5) | Mikulski 006 § 4 | ❌ molecular-type binding with no long-range tail, not a Newton mechanism | no |
| Non-minimal coupling `xi` or a bare `B` to reach weak field under induced gravity | Vantasner P246 attempt 0009 (`xi` near -148.8, or `B` about 893 times the induced term) | 🔶 the named escape of the induced-gravity row; a frontier, not a sign repair | marginal |
| Two-defect states under a non-rigid clock convention (only the clock-clock cross term `C(r)` flips sign between ensembles; the static dressed pair has no ensemble) | nobody | 🔶 untested | YES, and it is downstream of R13-W |

### 5.2 Diverging omega (the clock frequency must be a property of the object, not of the box)

| Route | Tried by | Outcome | Open? |
| --- | --- | --- | --- |
| Rigid isorotation of the certified vacuum frame | OpenWave R4, R7, R10, R12 (`kin ~ kappa L`, `omega* L = 7.1`) | ❌ extensive: the generator does not decay and the defect's orientation tail falls as `1/r` (R7 B7 gradient `r^-0.983`), so the inertia is linear in L; the empty vacuum itself carries no kinetic energy (`F_0i = 0` where `d_i M = 0`) | no |
| Rigid internal so(3) rotation of a non-equivariant texture (spin channel) | Mikulski 009 (`I_pure ~ L^2.93`, "the whole interior turns") | ❌ VOLUME-extensive, a different mechanism from the row above | no |
| Rigid clock on the degenerate vacuum `delta = 0` (`diag(-g, 1, 0, 0)`) | OpenWave R7 N3, R8 S6, R9 B1/B3/B4 | ❌ the clock inertia is EXACTLY zero: a vacuum-annihilating generator annihilates the whole rigid hedgehog; the surviving smooth generator at `delta = 0` is a boost, not the clock (the "degenerate-vacuum escape" was CLOSED at R8) | no on the rigid family |
| Core-breaking texture on an axially equivariant background (the global rotation acts on the core only) | Mikulski 009 § 6a, 011 | 🔶 `ESTABLISHED_KINEMATIC`: `I ~ L^0.14` (the only box-independent inertia in the three searches), but hand-built, not selected by statics, and prescribed J buys peripheral inertia (011) | as a seed for the wall question, from the spin side |
| Projector current + Landau-de Gennes lift + spectral guard + auxiliary axis + axis lock (P239 F to K, the Derrick balance `omega = J/(2I)`) | Vantasner P239 attempts 0008 to 0014 | 🔶 no stationary branch passed the frozen gates (0012: `I` to 3286, `omega` to 0.003 on a permuted vacuum; 0014 non-stationary) | `NUMERICALLY_UNRESOLVED`, campaign still active |
| Field-dependent kinetic axis L1, timelike-current Skyrme L2, the `kappa n^2 omega^2 (lambda_theta - lambda_phi)^4` term, the derivative sextic `B^mu` | Vantasner P240 | ❌ L2 axis-blind and fails the zero-eigenline escape gate; L1 superseded by the spectral-Cartan base | no |
| Commuting radial split `diag(-g, 1, d, -d)` on the P239 action | Vantasner P249 route history | ❌ collapses exactly (`R = a^-1` sends every term to zero); the asymptotic pencil has a singular weight-two clock block | no on that action |
| `Pgrad`, `J1`, `J2` gradient / Coulomb-sector mixtures (C2) | OpenWave R2 | ❌ constant `omega^2` coefficient = a massless boost mode; cannot stabilize | no |
| Potential-only localizers (C3) | OpenWave R6 (orbit-blindness theorem + Derrick) | ❌ closed by theorem | no |
| Time-row gradient penalty `K_T` (C4) | OpenWave R7 (interior window in `c2`, inertia still extensive) | ❌ closed for G3 | no |
| Quartic `(I1)^2` = `(F.F)^2` (C5, the author's proposal) at fixed coefficient | OpenWave R8, R9, R11 (no continuum limit on the ansatz; 42 % drift; static energy driven negative); Mikulski 008 (raw eta form inert; fundamental reading runs away by `-gamma s^2`) | ❌ closed in the fundamental (Legendre) reading on both stacks | no as a fundamental term |
| `(I_1^G)^2` in the repaired metric, ENERGY-FUNCTIONAL reading | Mikulski 008, 012, 013 (interior well at omega = 0.35 on one 32^3 box, depth-certified through 24 cycles, at g = 8, 64, 512) | 🔶 `ESTABLISHED_KINEMATIC` on one box; box independence NEVER measured; whether the energy functional is the `H` of a well-posed `L` is the author's own open question | YES: an L-ladder at fixed h is cheap and decisive; the reading question is author-gated |
| Core-weighted intensive (nonlocal) quartic | Mikulski 007 (well at omega = 0.8, ~100 core sites) | 🔶 `ESTABLISHED_KINEMATIC`, one box, nonlocal, frozen tangent not translation-covariant; the `w -> c w` normalization objection applies | YES for an L-ladder; NO as a local action |
| Fundamental-reading grid `gamma (I_j)^2` in a gamma window (C10 etc.) | Mikulski 010, 013 (fixed-depth wells that do not certify in 24 cycles, no plateau) | 🔶 `NUMERICALLY_UNRESOLVED`, leaning refuted | marginal |
| Weighted / tapered clock flows (`a0 -> w(M) a0`) | OpenWave old R13 list; Mikulski 007 masks; Vantasner (rejected as imposed support) | ❌ kinematic only by the `w -> c w` argument; never a fixed-J clock | no |
| Confined (wall-pinned) clock, then de-boxed | Vantasner P240 0041 to 0043, P244, P247 (`omega_min` rises with the box; de-boxed energy grows without plateau) | ❌ `CANDIDATE_REFUTED` on the tested ladders | marginal (non-spherical, deeper boxes) |
| Exterior-degenerate vacuum + added charged scalar + locks (P249) | Vantasner P249 (theorem-level, zero numerics); OpenWave verified the exact algebra 7/7 | ✅ `PERIODIC_ORBIT_EXISTS` at theorem level ON THAT ACTION; not a statement about the certified 4x4 field; no numerical profile; the variational bridge unverified by us | YES: (a) the field map back to the 4x4 action, (b) a numerical profile, (c) the finite-shell bubble variant |
| Degenerate-vacuum variant of the certified action (`diag(-g, 1, delta, delta)`, exact SO(2)) | TESTED on the rigid family and closed: OpenWave R7 N3 / R8 S6 / R9 B3 (`kin = 0` exactly at `delta = 0`, the escape CLOSED); the whole Vantasner P239 to P247 line runs on the degenerate exterior `(g, 1, 0, 0)` and the commuting split collapses there (P249 route history). Untested only as a RELAXED fixed-J object on `L_cert` whose core lifts a `delta2 = delta3 != 0` degeneracy | 🔶 closed on the rigid family (the rigid clock is invisible everywhere, not only in the vacuum); open as the relaxed contrast case; the author's 2026-08-31 wall comment places the degeneracy on walls, not in the vacuum | only as the relaxed contrast case |
| Degenerate WALL convention: 3D constant-omega regions separated by 2D walls where the last two eigenvalues equalize (the author, 2026-08-31 14:55) | nobody has run the literal construct (no wall, interface, or `omega(x)` clock exists in any of the three records at the pins; grep-verified in both external repos). Two nearest analogs exist and shape R13-W: Mikulski 009 § 5, a masked generator `exp(theta m(x) W)` whose interface term `(d_i m)[W, M]` makes the angle non-cyclic and J a prescribed parameter rather than a Noether charge (the obstruction W2 meets where two rigid generators meet); and the core-confined rotation of 009 § 6a / 011 (finite inertia, not selected by statics) | ❌ RAN as R13-W (2026-09-02, [task record](../tasks/m5_32_task_details.md)): `ESTABLISHED_KINEMATIC` at best. Walls are tensionless in this action (every planar profile has `E_u = 0`), the phase field on the vacuum has no action at all, and the fixed-J descent of a core in a free degenerate shell never reaches a stationary point: the shell melts and the inertia of its inner flank inflates without bound (2x to 5000x, an eigenvalue zigzag of the (2,3) gap that `E_u` cannot see, `omega` 0.0096 at 12000 iterations, falling as `h^1.7`, box-independent), `E_J` rises with the shell radius, `dE/dJ != omega`. Mechanism, exact: the phase gradient has no cost of its own in `-4 I1` (a planar orientation twist in a non-commuting plane carries inertia at zero static cost; an eigenvalue ramp along a planar direction likewise), so the fixed-J functional has no minimizer on `L_cert` for ANY clock convention; W3 audited 32 claims (15 / 10 / 7, all applied) | no on `L_cert`; the question moves to actions with a phase-gradient cost |
| Negative Hamiltonian terms as the clock propulsion (the author's 2026-08-28 group message: negative terms in the Hamiltonian as the only clock propulsion the author sees, with the 1+1D toy arXiv:2501.04036) | this is the mechanism already present in `L_cert` via the signature (the negative time channel), and in Mikulski 002/003's boost quartic (a Mexican hat in omega); the R1 form-level infeasibility is exactly the statement that a constant-coefficient negative term also destabilizes the vacuum | 🔶 present, not new; the open part is the same as the two rows above (where the negative term lives: core only, or wall) | folded into R13-W |

## 6. Most promising, and unexplored

### 6.1 Ranking (one line each, the evidence in §§ 2 to 5)

| Rank | Candidate | Why it ranks here | What it lacks |
| --- | --- | --- | --- |
| 1 | P249 exterior-degenerate SO(2) clock (Vantasner, a NEW action: Sym(3) + complex scalar + locks, not the author's 4x4 field) | the only theorem-level localized clock in the three searches; exact algebra independently confirmed; its thresholds belong to that completion only (C-M5C-002 exclusion), and the parallel program's own route frontier lists the 4x4 dynamics (`E_M532_DYNAMICS`) as a remaining route | a field map to the certified 4x4 action; a profile; the variational bridge outside its own review; two-clock, Newton, and gravity all declared out of scope |
| 2 | The degenerate-wall convention on the certified 4x4 action (the author's 2026-08-31 mechanism) | the one author-proposed route with no direct test on any stack; it keeps the certified action and explains how one global phase can have region-dependent frequency. Caveats: its spin-side analog (Mikulski 009 § 6a / 011) did not survive statics selection, its degenerate-vacuum half is closed on the rigid family (R8 / R9), and W1 alone cannot refute it (only W2 can) | RAN 2026-09-02 (R13-W): closed, `ESTABLISHED_KINEMATIC` at best; see § 5.2 and § 6.2 outcome |
| 3 | `(I_1^G)^2` energy-functional clock (Mikulski 008 / 013) | the only convergence-certified localized clock on the original 4x4 FIELD, under the repaired-metric action (`G = eta + 2uu` on the matrix slots, the same `h` as our C2 family whose rigid clock R4 refuted) in a reading that is not yet an `L`; robust in depth | a box ladder (never run); the reading question (is E the H of a well-posed L) is author-gated |
| 4 | Fixed-J two-clock cross inertia `C(r) = A/r` (Vantasner P240 D, our R3.iii) | the one Newton route whose algebra is exact (audit script D.3): like-rotating clocks attract at fixed J iff `A > 0`, and repel at fixed omega. This is the clock-clock term only; the Newton sign that fails on `L_cert` is the STATIC dressed pair, which has no ensemble | two relaxed localized clocks to measure `A` on (nothing on record supports `A > 0`): downstream of rank 1 or 2 |
| 5 | Higher-order-in-`F` / `dM`-built coefficients for the sign (Mikulski 006 escape map; OpenWave R11 U2's named term `(F.F)^2`, dilation exponent 5) | the only class on the 4x4 field not closed for Newton, with one named term and a cheap direct test (the owed dilation test against a flipped static sector); on the Newton axis alone it sits above rank 4, which waits on two localized clocks nobody has | the test has not run |
| 2 (after R14) | the P250 structure on the 4x4 field under a MODIFIED potential (a split stiffness mu >= 5.6e-4 at the degenerate point, R14-D audit): a first-order Maxwell crossing with a wall and a bag law at the plane level | the only construction on the 4x4 field that reproduces P249/P250's exterior-at-rest clock; the modification is the author's own diagnosis (the lock) in our variables | a lattice bag (radii 54 to 1100 do not fit the certified boxes), the other eight directions of the field, and the author's gates 1 to 3 (exact Noether charge, principal symbol, constrained second variation) |
| 2 (after R15) | the author's projector object `L_P = -4 I1^h - [V4^dd + mu (lambda_2 - lambda_3)^2] + c_P K_P^23` on the degenerate vacuum (2026-09-05): admissible on the lattice (R15-M), predictions (i) and (ii) hold, the tilt channel and the floor witness exact (R15-H, R15-V) | the only author-proposed object with an exterior at rest by construction that survives its own admissibility test; the H-adjoint kinetic term is bounded below on the vacuum twist where the certified term is not | a coexistence wall (the onset is continuous, a theorem, R15-P-iii); a fixed-J minimizer (the descent stalls without one, and the (1,2) sheet is free for `K_P^23`); the two definitions (`P23` off degeneracy, the split's completion) and an exact `-4 I1^h` gradient |
| closed (R14) | the two-derivative class K_T, K_lambda, R_{eta M eta}, R_hcov, K_P^h, T1..T4 (with or without the quartics) on the frozen rows (CLASS_INFEASIBLE, exact certificate); K_P^h as a clock on the certified vacuum (the vacuum ticks, omega ~ J/L^3); R_G as a Coulomb-compatible Newton mediator (threshold, g-dependent, no pair law) | | |
| closed (R15) | the coexistence wall on `L_P` under the invariant split (CONTINUOUS_ONSET, a theorem); the Ising wall between split orientations (a Goldstone saddle) | | |
| closed | everything at constant coefficient and current order, on all three stacks; global sign flips; induced gravity as a weak-field mechanism; potential-only and taper localizers; the rigid clock on any 4x4 vacuum, degenerate or not; wall-pinned clocks on the tested ladders (deeper-box existence open per C-M5S-015); the degenerate-wall convention on `L_cert` (R13-W, 2026-09-02) and with it every fixed-J minimization on `L_cert` in the full field space (free inertia, W0 S5) | see §§ 5.1 to 5.2 | |

### 6.2 The next OpenWave rung: R13-W, in the parallel program's obligation shape

Posted before any number is produced (their analytic-closure rule; our pre-registration rule). Every line below is fixed before the scripts are written; the scripts carry the same gates verbatim.

| Field | R13-W |
| --- | --- |
| Object | the certified 4x4 field `M(x)` under `L_cert = -4 I1 - V4`, vacuum `diag(-g, 1, delta, 0)`, g = 8, delta = 0.3 (continuity with R10 to R12), one g = 32 control |
| Symmetry or conservation license | the isorotation `R(q)` in the (2,3) eigenplane is NOT a symmetry of the certified vacuum (trivial stabilizer, R9). The wall hypothesis: on a 2D surface where the last two eigenvalues equalize, `a0 = [G1, M] = (d2 - d3)(E23 + E32) = 0` (audit script D.1), so the isorotation acts trivially ON the wall. Its jets do not vanish there unless `d_z(d2 - d3) = 0` as well, so two global phases `omega t` and `omega' t` meeting at the wall give an interface energy that is a PERIODIC function of the relative angle `Delta q = (omega - omega') t`, never a secular one, only if the wall profile absorbs the twist; that is exactly what W2 measures. The exact identity plus the derivative condition are the first check (W0, sympy) |
| Ensemble | fixed J (the ensemble in which two-clock attraction is possible, P240 0034 / R3.iii) |
| Variational functional | W1: `E_stat[M]` on a 1D profile `M(z)`, vacuum at both ends, the degeneracy `d2 = d3` constrained at z = 0, then released. W2: on a slab geometry with the wall at z = 0 and the generator `a0` rigid on each side, two functionals: (i) the static interface energy `E_stat(Delta q)` as a function of the relative angle over one period, and (ii) the wall-localized inertia `kin_wall` = the coefficient of `omega^2` in the slab (the empty vacuum contributes exactly zero, `F_0i = 0` where `dM = 0`, so every `omega^2` term in a defect-free slab is the wall's). W3: `E_J = E_stat + J^2/(4 kin_in)` with the ring or hedgehog core inside a spherical degenerate shell of radius R, the shell free |
| Admissible space | lattice fields on n^3 boxes at h in {1.5, 1.0, 0.75} and L in {48, 72, 96} varied independently (Layer 2 of the review); no taper, mask, or pinned interior; the vacuum shell pinned at the box edge only |
| Representation coverage | W1 and W2 cover the wall as a stationary object of the STATIC functional plus the stationary two-frequency read; they do not cover the dynamical question (does a wall form spontaneously under time evolution), because this stack has no Hamiltonian time integrator (FIRE is damped descent). That question is logged `NUMERICALLY_UNRESOLVED` unless a leapfrog on `L_cert` is built (a separate rung) |
| Observables | W1: tension `sigma_0 = (E_stat[wall] - E_stat[vacuum]) / area`, h-convergence order over three refinements, and whether the released profile keeps `d2 = d3` at z = 0. W2: `E_stat(Delta q)` periodic in `Delta q` with no secular growth (pass) or a twist energy growing with the accumulated angle (fail); `kin_wall` per unit area and its observed L-exponent across three boxes. W3: `R*`, `omega*`, `dE/dJ = omega` closure, and whether the shell survives fixed-J relaxation |
| Numerical representation (frozen before running) | FIRE from the certified stack, `dt0 = 0.01`, `dt_max = 0.1`, 12000 iterations or `fmax < 1e-6`; energies from `certified_energy`; the degeneracy constraint imposed by projecting the (2,3) block of `M(z=0)` onto its trace part; the two-frequency energy read from `omega_decompose` per half-space |
| Permitted verdicts | W1 pass: `ESTABLISHED_KINEMATIC` (a wall with finite, h-converged `sigma_0`). W1 fail (released profile melts, `sigma_0 -> 0`): the wall is not a static object; W2 decides whether the mismatch load pins it. W1 alone never refutes the convention. W2 pass: `ESTABLISHED_KINEMATIC` for the decoupling (`E_stat(Delta q)` periodic, `kin_wall` per unit area L-independent). W2 fail (secular twist energy, or `kin_wall` growing with L): `CANDIDATE_REFUTED` for the wall convention on `L_cert`, W3 does not run. W3 pass: `PERIODIC_ORBIT_EXISTS` at the relaxed-field level (not theorem level). W3 fail (shell melts or `R*` tracks L): `ESTABLISHED_KINEMATIC` at best |
| Failure scope | a refutation refutes the wall convention on the certified action as represented (rigid-per-region generator, static wall); it does not refute the author's mechanism on a modified action, nor the dynamical formation question |
| Analytic closure carried in | bag-level energetics in this document's own convention `E_J = E_stat + J^2/(4 kin)`: with the measured rigid law for a defect's interior cut at R, `kin_in = kappa R` (R7 B7, the `1/r` tail integrated to R), `E(R) = J^2/(4 kappa R) + 4 pi sigma R^2` gives `R*^3 = J^2/(32 pi kappa sigma)`; with a volume-filling `kin_in = kappa R^3` it gives `R*^5 = 3 J^2/(32 pi kappa sigma)` (audit script D.2, both minima). Either way `R*` is finite for any `sigma > 0`, so a finite radius at W3 is guaranteed at the bag level and proves nothing by itself; W3's content is the shell's stability under the field equations. The vacuum's kinetic softness (`F_0i = 0` where `dM = 0`) means an "ambient omega" costs nothing in the EMPTY vacuum of this action; in the author's 2026-08-31 picture the ambient frequency is set by content (a medium with `d_i M != 0` almost everywhere), where it costs the defects' inertia; only defects and walls carry inertia |
| Unlocks | if W2 passes: W3, then the fixed-J two-clock cross inertia on two bubbles (the Newton route ranked 4); if W1 or W2 fails: the RELAXED degenerate-vacuum contrast case (the rigid one is closed, § 5.2), and the P249 field-map question |

**Outcome (2026-09-02, the rung ran the same day; [task record](../tasks/m5_32_task_details.md), audit in `m5_32_r13w_audit.py`).** W0 closure 22/22 with four theorems the packet had not anticipated: every planar profile has `E_u = 0` (walls are tensionless, `sigma_0 = h V4`), the phase field on the uniform vacuum has no action at all, a planar orientation twist in a non-commuting plane carries inertia at zero static cost (so the fixed-J functional has no minimizer on `L_cert`), and the shell sharpens to the lattice floor. W1 FAIL as pre-registered (`sigma_0 ~ h^1.000 -> 0`, the released layer unstable). W2 PASS on the frozen gates, both of which turned out to be identities of any planar slab (unfalsifiable on that geometry, no evidential weight). W3 FAIL: twenty fixed-J relaxations, none stationary, the shell melting, the inertia of its inner flank inflating without bound in an eigenvalue zigzag (the W0 S2 density at lattice scale), `E_J` rising with the shell radius, `omega` falling with iterations and as `h^1.7`, box-independent. Verdict: the wall convention on the certified action is `ESTABLISHED_KINEMATIC` at best; the omega issue is reframed as the absence of a phase-gradient cost in `-4 I1`.

Owed OpenWave items unchanged and NOT in this rung: the 12000-iteration ring ladder, the relaxed-ring fixed J with a box-scaled taper, the `(F.F)^2` dilation test against a flipped static sector, the term catalog, the g = 32 relaxation (PAUSE RECORD 3). One item from § 5.2 is cheap and decisive for a rank-3 candidate and belongs to its author's stack, not ours: the L-ladder of the Mikulski 008 well.

### 6.3 The next OpenWave rung ladder: R14, the two-derivative class and the coexistence conjecture (a one-day autonomous run)

Posted before any number is produced. The inputs that reorder the ranking after R13-W, both received on the coordination thread: the model author's 2026-09-03 analysis (with an AI assistant; treated as evidence, not resolution, until R14-0 confirms it with our own scripts) and the parallel program's P250 (verified by our [`m5_32_p250_check.py`](../scripts/m5_32_p250_check.py), 24/24). Three facts move the plan: (1) P249's whole radiation edge comes from the explicit axis lock, and on the 4x4 degenerate vacuum four of the five Goldstones are clock-charged, so a strict fixed-J minimizer is excluded in any orientation-invariant M5 (rank 1's field map would inherit massless charged channels); (2) the two-derivative terms `K_lambda` (eigenvalue-only kinetic), `R_G` (the mixed-trace, Einstein-Hilbert-like contraction, a total derivative on every fixed-eigenvalue Lorentz orbit) and `K_P` (the projector kinetic term with phase stiffness `(lambda_2 - lambda_3)^2 |d phi|^2` on the (2,3) block) are LOWER order than R1's quartic basis and were never closed on any stack; `K_P` is exactly the phase-gradient cost R13-W found missing in `-4 I1`, and it is the inertia structure `iota = f^2 + 4 b^2` that P250's bag runs on; (3) the author's conjecture: within local second-order covariant Lagrangians a strict fixed-J minimizer clock and a Coulomb hedgehog cannot coexist, because the R13-W sheet and the hedgehog tail live in the same tilt channel and every two-derivative term that charges one diverges on the other. R14 tests the conjecture as a linear program over a frozen term basis, and runs the decisive numeric test on the named candidate regardless of the LP's answer. Scripts the author mentioned as attached did not reach the thread (Discussions cannot carry `.py`); when posted they become the audit baseline, R14-0 builds ours independently either way.

| Field | R14 |
| --- | --- |
| Object | the certified 4x4 field under `L_cert = -4 I1 - V4` plus a coefficient vector `c` on the frozen basis `B` below; vacuum `diag(-g, 1, delta, 0)`, g = 8, delta = 0.3 (continuity with R10 to R13), one g = 32 control where a rung's instrument has one |
| The frozen basis `B` | R1's thirteen (C0 + C1 with the parity-odd triple, the C2 insertions `I1_h`, `J1`, `J2`, `Pgrad`), R7's `K_T`, and the four two-derivative entrants `K_lambda = (1/2) eta^{mu nu} sum_a d_mu lambda_a d_nu lambda_a`, `R_eta`, `R_{eta M eta}` (the mixed trace `sum G_cd [d_mu M^{nu c} d_nu M^{mu d} - d_mu M^{mu c} d_nu M^{nu d}]` for two `G`), `K_P = (1/2) eta^{mu nu} tr(P d_mu N P . P d_nu N P)`, `N = M eta`, `P = (N - g)(N - 1)`; plus the four-derivative non-`F` quartics of class C6, `[tr(d_mu M eta d^mu M eta)]^2` and `tr(d_mu M d_nu M) tr(d^mu M d^nu M)`, and `(F.F)^2` (class C5, R11 U2). Every entrant gets the R0 selftests (one sympy definition, one numpy implementation on the certified stencil, complex-step gradient, a wrong-sign mutant that reddens) before it enters a constraint |
| Symmetry or conservation license | the clock is the (2,3) isorotation with the local generator `a0_local` (R12 H12-a, R13-W): not a symmetry of the certified vacuum (trivial stabilizer, R9), so every fixed-J number is a constrained read unless the candidate action makes the phase gradient cost, which is what `K_P` is for; the Goldstone count of the degenerate vacuum `diag(-g, 1, delta, delta)` (five, four clock-charged) is R14-0's first check |
| Ensemble | fixed J for the clock; the static pair for Newton (G2 as in R3), with the fixed-J two-clock read where two relaxed clocks exist |
| Witness families (the LP's rows; each measured per basis term on saved fields, never fitted) | (i) the hedgehog tail: the static integral of each term on the R10 relaxed seed (n32 L48 it12000) and on the n48 L72 and n64 L96 seeds, its L-exponent (a term whose tail integral grows with L enters the boundedness row with that exponent); (ii) the R13-W sheet: the W0 S5 (1,2)-twist family at widths `w` in {h, 2h, 4h, 8h} (h = 1.5) and the three saved W3 end fields (`R_s` = 9, J = 200 at 3000 and 12000 iterations; n48 L72 `R_s` = 15), each term's static density and its `omega^2` coefficient on them; (iii) the boost-dressed pair: each term's pair energy `E_int(d)` on the R3 arm-i ansatz at d in {8, 10, 12, 16} (the sign of `dE/dd` per term is a linear functional of `c`); (iv) kinetic positivity: R1's per-channel form matrices `H_2` on the nine time channels (the Farkas machinery of R1, extended to the entrants); (v) the Coulomb footprint: the static-sector identity on block-diagonal fields (exact per term) and the like-charge sign on the R1 pair instrument |
| Variational functionals and gates | R14-0 VERIFY (each a PASS/FAIL that a named mutation fails): the P249 Hessian split by `V_ldg / V_axis / V_lock` in the (a, t, p, u, v, q) chart reproduces `(5, 6, 6, 0, 0, 6) / (0, 4, 4, 4, 4, 4) / (0, 0, 12, 0, 0, 12)`; five Goldstones on `diag(-g, 1, delta, delta)` with charges (0, 1, 1, 1, 1); `R_eta` has `EL == 0` for generic `M` (1+1 and 2+1, sympy) and `R_G` integrates to zero on fixed-eigenvalue orbits with spectral convergence for the other `G` while an eigenvalue-gradient mutant does not; `K_lambda` inert on orientation gradients; `K_P` blind to tilts and boosts, phase stiffness `(lambda_2 - lambda_3)^2` on the (2,3) block; `K_P` does NOT charge the sheet (its density on family (ii) and on the W3 zigzag fields against `I1`'s); covariance of every entrant (G4 machinery, the no-eta control failing). R14-A THEOREM: the LP `find c` with `c_I1 = -4` fixed, `sum \|c_k\| <= 1` on the entrants, rows: (i) every L-divergent tail combination = 0; (ii) `E_stat(w) w^3 >= epsilon` on the sheet family at the smallest `w` (the static cost must outgrow the `1/w` inertia) and `E_stat >= 0` on every member; (iii) `E_int(16) - E_int(8) > 0` on the dressed pair (attraction) with the like-charge pair sign unchanged (v); (iv) `H_2 >= 0` on every channel. Output: a feasible cone (its vertices, at most three carried forward) or the dual infeasibility certificate, and the same LP on the sub-bases (two-derivative only; four-derivative `F`-built only; the non-`F` quartics only) to name the class that carries or blocks feasibility. R14-B DECISIVE NUMERIC (always runs, on `L_cert + c K_P` at `c` in {0.3, 1, 3} and on every LP vertex): the fixed-J continuation protocol, the R13-W W3 machinery with the local generator on the relaxed hedgehog, h in {1.5, 1.0, 0.75} x L in {48, 72, 96} at J = 200 (J = 50 and 800 on the base box), 6000 iterations or `fmax < 1e-6`, the frozen-generator control, the G5 boundedness probes (sawtooth and wide-dressing families, no guard). R14-C NEWTON (the scalar channel; runs on spare cores from the start): `R_G` on the dressed pair (R3 arms i and ii, n32, both boundary types, `c_R` in {-1, -0.3, 0.3, 1}): the sign of `dE/dd` against `sign(c_R)` with the static 3x3 record unchanged to machine precision; `K_lambda` with a light scale `m_s` in {0.1, 0.3, 1}: the linearized eigenvalue-channel exchange between two relaxed cores (sign and range) and the multipole content of the relaxed hedgehog's eigenvalue-deficit far field (monopole means `1/d`, dipole `1/d^2`). R14-D THE P250 BRIDGE: on `L_cert + c K_P` (and `+ K_lambda`), the rotating-frame potential over the (2,3) split, `V_omega(s) = V4(s) - (omega^2/2) iota(s)`: does a clock-active bulk minimum of negative depth exist, with a Maxwell crossing `omega_*^2` where its depth equals the vacuum's; if it does, the 1D coexistence kink and its tension (our W1 machinery between two phases) and the thin-wall bag law, i.e. P250's comparison object on the 4x4 field. R14-E SYNTHESIS: ledger §§ 5 and 6 updated, the term catalog (owed since PAUSE RECORD 2) seeded from `B` with every measured exponent, the method-note section, the thread post |
| Admissible space | lattice fields on the boxes above; the saved fields of R10 and R13-W as witnesses (regenerable, commands in the task record); no taper, mask, or pinned interior; the vacuum pinned at the box edge only; the LP over real coefficients with the normalization stated |
| Representation coverage | R14 covers local covariant terms of `M` alone up to four derivatives at first order in `d` (no second-derivative Lovelock-type invariants: their scaling pass is the first fallback below); the static pair and the fixed-J clock; no time integrator, so the dynamical questions (Floquet lifetime of a metastable state, wall formation) stay `NUMERICALLY_UNRESOLVED` |
| Numerical representation (frozen) | FIRE from the certified stack (`dt0 = 0.01`, `dt_max = 0.1`) with the audited `fire_proj` wrapper (plateau fix applied); energies from the registry with each entrant's selftest; the LP by two solvers (HiGHS simplex and interior point) with the certificate re-checked in exact rational arithmetic on the rounded rows; every family value carries its box, stencil and seed maturity |
| Permitted verdicts | R14-0: CONFIRMED / QUALIFIED / REFUTED per statement of the author's analysis. R14-A: `CLASS_INFEASIBLE` (the certificate = the conjecture as a theorem within `B`) or `CONE_FEASIBLE` (vertices named). R14-B pass: `PERIODIC_ORBIT_EXISTS` at the relaxed-field level (`E_J` and `omega*` converge on the ladder: the two finest rungs in each direction within 10 percent, drift shrinking monotonically); fail: `CANDIDATE_REFUTED` for that action (drift, runaway or melt), with the runaway exponent recorded. R14-C: G2-lite met (attraction sign robust across two arms, two boxes, both boundary types, Coulomb unchanged) or not, per term and coefficient sign. R14-D: `MAXWELL_CROSSING_EXISTS` (the bridge object constructed at the reduced level) or `NO_CLOCK_ACTIVE_BULK` at the scanned coefficients |
| Failure scope | an infeasible LP refutes the conjecture's negation within `B` only (the theorem is class-relative); a B-refutation refutes that action as represented (local generator, relaxed hedgehog seed), not the term on another object; a C failure closes the scalar-channel Newton route for those terms at those coefficients; nothing here refutes the author's mechanism on a modified potential (C3) or the Lovelock class |
| Fallbacks (the ladder never waits) | LP infeasible: B still runs on `K_P`, and R14-A' adds the second-derivative scaling pass (the author's F: `epsilon epsilon d d M d d M` invariants on the sheet, `~ Psi^2 / w^3`, and on the tail, `~ r^-6`) as the next basis, no ghost analysis this run; no Maxwell crossing in D: D' scans the potential axis (C3: the eigenvalue-penalty coefficient on the (2,3) pair) for one that opens it, reported as a modified action; a runaway in B at every `c`: the runaway exponent in `c` is the deliverable; an instrument defect: fixed as a logged deviation, the rung re-run, three tries then STOP on that gate |
| Analytic closure carried in | the Legendre reduction and `E_J = E_stat + J^2/(4 kin)` as in § 6.2; the W0 theorems S1 to S6 (planar flatness, phase flatness, free inertia, the sharpening bag) as the exact facts any candidate must overturn, each re-evaluated per entrant symbolically in R14-0 (a term that leaves them intact cannot change R13-W's verdict); the thin-wall laws of P250 verified in `m5_32_p250_check.py` |
| Unlocks | `CONE_FEASIBLE` and a B pass: the two-clock cross inertia on two relaxed clocks (rank 4) and the full G1 to G7 battery on the vertex; `CLASS_INFEASIBLE`: the theorem posted, the Lovelock class and the potential axis as the only exits within the author's admissibility boundary; a D crossing: the P250 field map at the reduced level, the bag on the 4x4 field as the next rung |
| Run shape | one autonomous session of about one day on the 16-core machine: R14-0 (1.5 h) then R14-A (4 h) in the foreground with R14-C's lattice jobs detached on 8 cores from the start; then R14-B (6 to 8 h, 8 cores) with R14-D (3 h) beside it; R14-E (2 h); one independent adversarial auditor per rung before its verdict steers the next (the plan's § 6 engine and § 7 stop rules apply unchanged: the `killswitch` keyword, the per-rung mobile notification with the totals, the free-rung budget); owed items run as fillers on idle cores (the 12000-iteration ring ladder, the g = 32 relaxation, the `(F.F)^2` dilation test as a scaling row of the LP) |

**Outcome (2026-09-05, the ladder ran 2026-09-04 23:42 to 2026-09-05 (see the [task record](../tasks/m5_32_task_details.md) R14 section for every number and the five audits).** R14-0 (audited 10/4/0): the author's 09-03 algebra holds except the orbit theorem, which is CONFIRMED on rotation orbits and single-plane boost textures and REFUTED on textures with two or more boost planes for G = eta M eta, M^-1 and h_cov (converged nonzero periodic integrals; the delta pairing of a derivative index with an internal index is invariant under global, not local, frame changes); our own findings: the plain K_P is indefinite off the vacuum spectrum (the author's 09-05 H-adjoint correction, confirmed independently, with the invariant order tr(Om^T H Om H^-1)), M^-1 is undefined on the singular certified vacuum, the certified vacuum TICKS under the local clock (K_P's inertia is volume-extensive), K_P's static energy on the hedgehog tail is L-divergent (exponent 1.34), and the R13-W free inertia survives the whole two-derivative set (K_P, R_G, K_lambda all have zero static cost on the twist sheet: no fixed-J minimizer on L_cert plus any of them, by theorem). R14-A (audited 4/4/1): on the frozen rows (two tail fields, both sheets, both pair reads, the Coulomb repulsion and 1/d form, fifteen positivity channels, the exact UV quadratic forms in seven directions with cutting planes, the exact quartic forms), the two-derivative class {K_T, K_lambda, R_{eta M eta}, R_hcov, K_P^h, T1..T4}, with or without the quartics, is CLASS_INFEASIBLE with an exact rational Farkas certificate on both the producer's and the auditor's assembly: the binding structure is the like-charge 1/d form against the two tails (with the zigzag sheet or the relaxed pair); the full basis with R1's F-built thirteen has no point below coefficient norm 100 on either assembly and only forty-to-hundredfold four-derivative corners above it where the linear-response rows are not valid; the zigzag sheet is the row that raises the norm (without it a point exists at 1.4); the certified 4 I1's omega^2 coefficient is negative on the hedgehog boost tangents, so every feasible point carries K_T >= 0.064 to repair it. R14-B (audited 4/3/1): CANDIDATE_REFUTED on L_cert + c K_P^h at c = 1 and 3 for J in {50, 200, 800} on the h x L ladder (1.5/48, 1.0/48, 1.5/72): no run is stationary (every descent ends on a logarithmic plateau with dt collapsed), the fixed-J term is numerically invisible, and the frequency omega = J / (2 kin) is a lattice cell count (K_P^h's inertia is extensive in cells, 0.63 to 0.71 of the vacuum value per free cell with a third to half in the pinned shell; the n48 L48 and L72 end fields are the same lattice-index function, so the descent is h-blind and my L^-3 reading was an h^3 identity); the pre-registered (2,3) closure started in the first hundred iterations (gap 0.30 to 0.24, K_P^h down 40 to 60 percent) and stalled; the three c values are one trajectory at three speeds, and every descent stayed in the unsampled boost-free subspace M_0i = 0, a saddle at c = 0.3 (undecided there); the author's verdict prediction holds, the mechanism is not the sheet but the exterior's extensive K_P^h inertia that removes the clock before any sheet forms. R14-C (audited 1/4/4, an instrument defect found and fixed, the c_R = +-1 heals rerun): on the relaxed g = 32 dressed pairs the pair-energy slope is (certified slope) + c_R (R_G slope) = -882 + 2058 c_R at lambda 0 and -2316 + 2058 c_R at lambda 1, so the sign follows c_R only above c_R = 0.43 at lambda 0 and never within |c_R| <= 1 at lambda 1, the R_G slope scales with g, the static 3x3 record is changed (-5.54 per unit against +0.28), and on the ansatz the R_G pair energy is a boundary-flux term with no power law (its slope grows with d): the author's "sign follows sign(c_R), d^-2, record untouched" is QUALIFIED, REFUTED, REFUTED; K_lambda gives an attractive Yukawa only with an assumed light mass that V4 does not supply (on the certified action its pair energy is a core overlap with exponent 6). R14-D (audited 5/1/2): on L_cert + c K_P^h the exterior ticks, V_eff at fixed omega is unbounded along the split (sealed behind eigenvalue collisions) and a far-split clock-active pocket exists (a saddle-node at omega 1.0e-3), so the P250 object (an exterior at rest) does not exist with the certified potential; on the C3-modified potential V4 + mu (m2 - m3)^2 with mu >= 5.6e-4 a FIRST-order Maxwell crossing exists at the plane level (an exterior at rest at the diagonal minimum 0.157, a rotating interior off the fixed-sum line, tension 0.63 to 4.0, thin-wall radius 1100 down to 54), the P250 comparison object on the 4x4 field under a modified action. Net for the ledger: the two-derivative class is closed by theorem on the frozen rows; K_P^h is closed as a clock candidate on the certified vacuum (the vacuum ticks); R_G is a Newton coupling with a threshold and a g-dependent strength and no Coulomb-compatible pair law; the only construction that reproduces P250's structure on the 4x4 field is a modified potential with a split stiffness at the degenerate point, which is the author's own section-1 diagnosis (the lock is the whole gap) in our variables. The term catalog owed since PAUSE RECORD 2 is now [`m5_32_term_catalog.md`](m5_32_term_catalog.md).

### 6.4 The next OpenWave rung ladder: R15, the floor witness, the tilt channel, and the author's projector object (an overnight autonomous run)

Posted before any number is produced (go 2026-09-05 19:34 UTC). The input that sets this ladder: the model author's 2026-09-05 16:54 reply on the coordination thread ([`m5_32_convo.md`](../tasks/m5_32_convo.md), the 16:54 entry; AI-assisted, treated as evidence, not resolution, until our scripts reproduce each number). It accepts every R14 verdict, withdraws the `R_G` orbit theorem, and does three things R15 answers: (1) it repeats one request, "V1", the 09-05 floor witness (a twist of the spatial frame INSIDE a boost-dressed frame makes the certified `-4 I1` unbounded below, `U = -2 b^2 k^2 g^2 (delta - 1)^2` at leading order in g, the H-adjoint `-4 I1^h` bounded, `+2 b^2 k^2 g^2 (delta - 1)^2`), a direction no R14 descent sampled; (2) it states a hyperbolicity condition for the tilt channel on a rotating background (`<F_0z, F_0z> = 4 k^2 omega^2 s^2 (delta + s - 1)^2`, no kinetic term, hyperbolic iff `w kappa_2 > 16 omega^2 s^2` after a core-weighted regulator `w(M) tr(dM G dM G)`), which is the R15-H gate we had held; (3) it pre-registers a NEW object with four predictions. R14 closed the two-derivative class on the frozen rows and `K_P^h` on the certified vacuum; the author's object leaves both (a new vacuum and an exact projector), so R15 is the first rung ladder off the certified potential, run under the same admissibility discipline (every number on the new vacuum re-established, never carried over from the certified record). Two definitions in the reply are author-gated and are asked with the results; R15 runs under our stated readings so nothing waits.

| Field | R15 |
| --- | --- |
| Objects | (a) `L_cert = -4 I1 - V4` on the certified vacuum `diag(g, 1, delta, 0)` (code branch s = -1; N-spectrum `(-g, 1, delta, 0)`), g = 8, delta = 0.3, for R15-V and the I1 half of R15-H; (b) the author's projector object under OUR READING, `L_P = -4 I1 - V4^dd - mu (lambda_2 - lambda_3)^2 + c_P K_P^23`: `V4^dd` the certified trace-target form `W1 sum_p (tr N^p - C_p)^2` with `C_p = (-g)^p + 1 + 2 delta^p` (the degenerate vacuum `diag(g, 1, delta, delta)`), the split stiffness written through symmetric functions `(lambda_2 - lambda_3)^2 = s^2 - 4 p`, `s = tr N - lambda_g - lambda_1`, `p = e_2(N) - lambda_g lambda_1 - (lambda_g + lambda_1) s` (smooth at the degenerate point, no pair eigenvectors), and `K_P^23` the E-density `(1/2) [sum_i tr(Om_i^T eta Om_i eta) + omega^2 tr(Om_0^T eta Om_0 eta)]`, `Om_mu = P23 A_mu eta P23`, `P23 = I - P_g - P_1` with `P_g`, `P_1` the rank-one Lagrange projectors of the two isolated eigenvalues (a rational covariant function of N, equal to the author's `(N - g)(N - 1) / [(lambda_23 - g)(lambda_23 - 1)]` at `lambda_2 = lambda_3` and well defined off it, where the author's formula is not a projector: author-gated question 1). Theorem carried in (checked numerically in the selftest): on the projected block the H-adjoint form equals the eta form, `tr(Om^T H Om H^-1) = tr(Om^T eta Om eta)`, because `P23 u = 0` and `(eta u)^T P23 = 0`; so the reply's H-adjoint `K_P` and ours coincide. The author's kinetic term is `-4 I1^h`; R15-P relaxes under the certified `-4 I1` (the stack's exact gradient; the extended registry has no `I1^h` gradient) with `-4 I1^h` READ on every end field, and R15-V measures exactly the channel where the two differ |
| Symmetry or conservation license | on `diag(g, 1, delta, delta)` the (2,3) isorotation IS a symmetry of the vacuum (the stabilizer of the degenerate pair), so the exterior at rest is exact: the local clock generator `a0_local = J M - M J` vanishes identically there and both inertias (`I1`, `K_P^23`) are zero outside the split region (the author's prediction (i), a construction fact verified to machine precision, not a measurement); the fixed-J read is the constrained R13-W read inside the split core only |
| Ensemble | static energies on constructed fields (R15-V); the second variation on a rotating uniform background (R15-H, symbolic); static relaxations on the new potential (R15-M); the reduced rotating-frame functional (R15-P-iii); fixed J (R15-P-iv) |
| R15-V (the author's V1) | V-a SYMBOLIC: the static density of `-4 I1` and `-4 I1^h` on the homogeneous jets of `M = Lambda(b x_a) R(k x_c) D R^T Lambda^T` (a boost of rapidity slope b along axis a, a (1,2) twist of slope k along axis c inside it) for the nine (a, c) pairs, as polynomials in (b, k, g, delta) at second order in each: the author's `-2 b^2 k^2 g^2 (delta - 1)^2` (eta) against `+2 b^2 k^2 g^2 (delta - 1)^2` (H-adjoint) at leading order in g. V-b LATTICE: n64 L48 (h 0.75; also n32 L48 and n48 L72 for the h and L reads) on the uniform vacuum with a LOCALIZED radial boost `chi(r) = 0.5 exp(-r^2 / (2 w^2))`, w = 2, and the (1,2) twist `psi = k z`, k in {0.5, 1, 2}, in both orders (twist inside the dressed frame; twist after dressing, the author's co-rotation control): `Delta E = E[dressed + twist] - E[dressed]` for `E_u = -4 I1` (the certified E_u) and for `-4 I1^h`, with V4 read as the orbit control (it must not move); the same on the relaxed n32 L48 hedgehog dressed the same way. Claims tested: (V1) `Delta E_eta < 0` and growing as k^2 (unbounded); (V2) `Delta E_h > 0`; (V3) twist-after hides it (both positive); (V4) the symbolic coefficient |
| R15-H (the tilt channel) | sympy jets on the rotating uniform background `M(t) = R23(omega t) D_s R23(omega t)^T`, `D_s = diag(g, 1, delta + s, delta - s)`, with the (1,2) tilt `theta(t, z)` applied inside (`M = R23 R12(theta) D_s R12^T R23^T`): the second-order Lagrangian `L2 = a theta_t^2 + b theta_z^2 + c theta^2 + d theta_t theta_z` for `-4 I1`, `-4 I1^h`, `c_P K_P^23`, the split term, and the regulator `w tr(d_mu M G d^mu M G)` for G in {eta, h_cov}; the principal symbol per term. Claims tested: (H1) no `theta_t^2` from any curvature term on a uniform rotating background (the author's "no kinetic term"); (H2) the `-4 I1^h` coefficient of `omega^2 k^2 theta^2` equals `16 s^2 (delta + s - 1)^2` up to the sign convention (the author's `<F_0z, F_0z> = 4 k^2 omega^2 s^2 (delta + s - 1)^2`); (H3) with the regulator the tilt equation is hyperbolic iff the net `theta_z^2` and `theta_t^2` coefficients have the wave sign, reported as an inequality in our variables and compared with `w kappa_2 > 16 omega^2 s^2` under the identification `kappa_2` = the regulator's tilt stiffness per unit w (author-gated question 2: s, k, kappa_2, w, G); (H4) `K_P^23` is blind to the (1,2) tilt and to the (1,2) twist sheet (zero static cost and zero inertia, symbolically and on the lattice S5 family), which by the R13-W free-inertia theorem decides prediction (iv) before any descent |
| R15-M (admissibility of the new vacuum) | M-a: the Hessian of `V4^dd + mu (lambda_2 - lambda_3)^2` at the vacuum in the ten symmetric directions for mu in {0, 1e-3, 1e-2, 1e-1}: the null count (five orbit directions plus the quartically soft split doublet at mu = 0; the doublet lifted by mu) and the split mass. M-b: the uniaxial radial hedgehog (leading eigenvector radial; the (2,3) frame has nothing to wind) relaxed under `L_P` at c_P = 0 (the statics do not see `K_P^23` at omega = 0 only through its gradient term, which IS present: c_P in {0, 1}) and mu in {0, 1e-2}, n32 L48 3000 it and n48 L72 3000 it (n64 L96 if the cores allow): localized (E_u converging on the L ladder, a core radius) or not; the eigenvalue profile `lambda_a(r)`; the E_u tail exponent. The certified biaxial hedgehog (R10) is a DIFFERENT object and its record does not transfer: stated, not assumed |
| R15-P (the author's four predictions) | (i) verified in the license row (exact). (iii) REDUCED WALL: diagonal planar profiles `M(z) = diag(g, 1, m2, m3)`, `F[m2, m3] = int dz { (c_P / 2) (m2'^2 + m3'^2) + V4^dd + mu (m2 - m3)^2 - omega^2 [c_P kappa_P (m2 - m3)^2 + 8 (m2 - m3)^2 (m2' - m3')^2] }` (`kappa_P` the exact reduced inertia coefficient of `K_P^23`, read from the registry on a slab, expected 1), the exterior at the true vacuum `(delta, delta)`; scan mu in {1e-3, 1e-2, 1e-1} x c_P in {0.1, 1, 10}: the plane-level transition at the onset `omega_c^2 = mu / (c_P kappa_P)` is CONTINUOUS (our pre-registered expectation: `V4^dd` is quartically soft along the split, so `V_eff = a s^4 + (mu - c_P kappa_P omega^2) s^2` has a second-order onset unless the off-line (trace) direction opens a first-order crossing as R14-D found) or FIRST-ORDER (a Maxwell crossing with two coexisting minima); if first-order: the wall relaxed, its tension, and the 10-to-90 width against `(c_P / mu)^(1/2)` (the author's O(10) box units at `c_P ~ 100 mu`); if continuous: the onset and the linear correlation length. (ii) TAIL: `K_P^23` static energy on the R15-M hedgehogs at L = 48, 72 (96): the L-exponent (the author: 0; the certified `K_P^h` on the biaxial hedgehog: 1.34). (iv) FIXED J: `E_J = E_stat + J^2 / (4 kin_tot)`, `kin_tot = kin_I1 + c_P kin_KP23` with the local generator, on the R15-M hedgehog at c_P = 1, mu = 1e-2, J = 200, n32 L48 3000 it then n48 L72 (the two-rung ladder), the R14-B diagnostics (stationarity, `omega = J / (2 kin_tot)`, the split profile, the (1,2) sheet read); the author's expectation: no minimizer; ours: decided by H4 (blind: the free (1,2) inertia survives, no minimizer by theorem; charged: the descent is a search) |
| Admissible space | lattice fields on the boxes above, the vacuum pinned at the box edge only (shell 1.6), no taper, mask or pinned interior; the reduced functional on a pinned long line; symbolic jets at a point |
| Representation coverage | static and rotating-frame reads, one fixed-J ladder of two rungs; no time integrator (the regulator's 3+1 run stays `NUMERICALLY_UNRESOLVED`); `-4 I1^h` read, not relaxed |
| Numerical representation (frozen) | the certified stack (sym stencil, exact adjoints, FIRE dt0 0.01, dt_max 0.1) with the trace targets `C_p` of the degenerate vacuum; the new pieces (`K_P^23` static and kinetic with exact gradients through the jets and the projector, the split term) each with a complex-step gradient gate at 1e-8 relative and a central-difference gate, the projector identity `P23^2 = P23`, `P23 u = 0`, the H-adjoint equality, covariance under random boosts and rotations with a no-eta control failing; the reduced functional with L-BFGS and the slab cross-check of R14-D2; sympy for V-a and H |
| Permitted verdicts | R15-V: CONFIRMED / QUALIFIED / REFUTED per statement V1 to V4. R15-H: per statement H1 to H4. R15-M: `ADMISSIBLE` (a localized static hedgehog with converging E_u on the ladder) or `NOT_LOCALIZED` per (mu, c_P). R15-P-iii: `FIRST_ORDER_CROSSING` (wall, tension, width) or `CONTINUOUS_ONSET`. R15-P-ii: the exponent with its two-box uncertainty. R15-P-iv: `PERIODIC_ORBIT_EXISTS` (the R14-B convergence gates) or `CANDIDATE_REFUTED` (drift, melt, runaway) or `BLIND_BY_THEOREM` (H4 blind and the descent shows the free-inertia drift) |
| Failure scope | V confirmed refutes nothing of the R14 verdicts (they were reached on the sampled sector) but adds an unbounded-functional caveat to every `L_cert` dressed-core number (R3, R4, R13-W, R14-C/D); an M failure closes the author's object as represented (the uniaxial hedgehog on the trace-target potential), not on another potential of the same vacuum; a P-iv refutation is of `L_P` under our reading with `-4 I1` (not `-4 I1^h`) and the local generator |
| Fallbacks (the ladder never waits) | V-b on n64 too slow: n48 L48 carries the ladder; M not localized at mu = 1e-2: mu = 1e-1 and 1 tried, the best available field carries P-ii and P-iv with the caveat; P-iii continuous everywhere: the onset table is the deliverable and the width question is answered by the correlation length; P-iv runaway: the exponent; an instrument defect: fixed as a logged deviation, the rung re-run, three tries then STOP on that gate |
| Analytic closure carried in | the projector identities and the H-adjoint equality (this packet); the R13-W W0 theorems (planar flatness, free inertia); R14-0's Goldstone count on the degenerate vacuum (five, charges (0, 1, 1, 1, 1)); the thin-wall laws of R14-D2 |
| Unlocks | V confirmed: the `-4 I1^h` stack (a gradient for `I1^h`) as the next instrument; H3 hyperbolic for some w: a 3+1 integrator question, posted; M admissible and P-iii first-order and P-iv a periodic orbit: the full G1 to G7 battery on `L_P`; any P failure: the author's object closed under our reading, the two questions decide whether a second reading exists |
| Run shape | one autonomous overnight session on the 16-core machine: R15-V-a and R15-H symbolic in the foreground (1.5 h) with R15-M's relaxations detached on 8 cores from the start; R15-V-b lattice (1 h); R15-P-iii (1 h); then R15-P-ii on the M fields and R15-P-iv (5 to 6 h, 8 cores); one independent adversarial auditor per rung (definitions and claims only, never the producer's scripts) before its verdict steers the next; the `killswitch` keyword, the per-rung mobile notification, the resume ping at reset + 5 with the watchdog; the results post on #186 in the TL;DR format with the two author-gated questions and the scripts ask in the body |

**Outcome (2026-09-06, the ladder ran 2026-09-05 20:18 UTC to 2026-09-06 (see the [task record](../tasks/m5_32_task_details.md) R15 section for every number and the five audits).** R15-V (audited 5/2/0): the author's floor witness V1 holds on the vacuum: at the jet level the certified `-4 I1` has static E-density `-8 (delta - 1)^2 (g + d_a)^2 b^2 k^2` on a (1,2) twist of gradient k inside a boost dressing of rapidity gradient b (`d_a` the vacuum entry on the boost axis; the author's `-2 g^2 (delta - 1)^2` is the large-g form at ratio exactly 4), independent of the base rapidity, and `-4 I1^h` is the exact sign flip; on the lattice with the author's radial profile (n64 L48) `DeltaE_eta = -515 / -1739 / -3583` against `DeltaE_h = +557 / +1835 / +4002` at k 0.5 / 1 / 2 (the author's signs and growth, our normalization); the twist after the dressing is positive in both forms but not equal (the author reports one number), and on the relaxed hedgehog BOTH forms go negative (the repair is a vacuum-frame statement); the dressed baseline is grid-divergent through the origin of the radial unit vector and the k growth is stencil-attenuated, the signs stable throughout. R15-H (audited 5/5): on the rotating split background every tilt-channel claim is exact: no `theta_t^2` from any curvature term or from `K_P^23`, `gamma(-4 I1) = 32 omega^2 s^2 (delta + s - 1)^2 theta_z^2`, the regulator alone supplies `alpha = 2 w (delta + s - 1)^2`, hyperbolic iff `w > 16 omega^2 s^2` (the author's inequality with `kappa_2` normalized out), and the static `K_P^23` is exactly zero on any (1,2) twist sheet, a free direction (no fixed-J minimizer along it by the R13-W theorem); the audit adds that the one-tilt ansatz drops the Coriolis partner (the k = 0 gap is ansatz-dependent) and that a (2,3) sheet is not free. R15-M (audited 2/4/0): the author's object is ADMISSIBLE on the lattice under our reading of `P23` (`I - P_g - P_1`) and of the invariant split: the Hessian null counts are the predicted 7 (mu 0) and 5 (mu > 0) with the split stiffness exactly `4 mu`; the uniaxial hedgehog relaxes on both boxes and all four (mu, c_P) to a finite-energy hedgehog with a `1/R` energy tail (`r^-4` density) whose exterior is the seed's texture (the descent reaches r < 14.9 only), the pair staying degenerate (max split 0.0135 at c_P 1), the (2,3) inertias tiny and equal to the split integral by identity; the certified reference's slower decay is the z-axis line of the singular seed frame, invisible on the degenerate vacuum. R15-P: (ii) TAIL_FINITE (`K_P^23 ~ r^-4.1`, L-exponent 0.08 to 0.11, the seed's `1/L`); (iii) CONTINUOUS_ONSET on all nine (mu, c) points, a theorem (`V4^dd >= 0` with equality only on the vacuum spectrum, so no Maxwell crossing below `omega_c^2 = mu / c` and the exterior unstable above it): there is no coexistence wall between an exterior at rest and a rotating interior on `L_P`, the diagonal-sector Ising wall is a Goldstone twist by the clock generator (the audit), and the static split decay length `(1/2) sqrt(c/mu)` is not what the hedgehog's split shows (2.1 to 2.4 box units regardless of c_P: the split is slaved to the core texture); under the diagonal-entry reading of the split term the audit finds no exterior at rest at any omega > 0, so the author's completion is asked; (iv) CANDIDATE_REFUTED (audited 3/2/0): at J = 200 the descent inflates the split in the innermost cells to buy inertia (`E_J` 3.7e5 to 89 within 600 iterations, the fixed-J term 43 of 89, 84 percent of the kinetic density in eight cells) and pins itself on the `lambda_1 = lambda_3` eigenvalue crossing, the branch cut of the ordered-label `P23` and of the leading-eigenvector clock (the audit: my stiff-valley curvature is a kink, the true `E_J` climbs a staircase as cells cross, a label-free reading carries a tenth of the reported inertia); n48 L72 replicates it (`E_J` 98.72, six cells on the crossing at r 1.3, gap 3.6e-6); both sides expected no minimizer, and the run refutes the candidate for a stronger reason: the fixed-J functional exploits the seam of the object's definition, which is the first question to the author. Net for the ledger: the floor witness and the tilt channel are symbolic facts on our stack; `L_P` is admissible and its predictions (i), (ii) hold, (iii) fails as a coexistence object under the invariant split, (iv) is refuted on the object as defined (the descent runs to the projector's branch cut) and waits on the author's two definitions.

### 6.5 The next OpenWave rung ladder: R16, the author's local-circle object v4 (PROPOSED 2026-09-06, HELD for the go)

The input that sets this ladder: the model author's four 2026-09-06 comments on the coordination thread ([`m5_32_convo.md`](../tasks/m5_32_convo.md), the 05:47 to 07:20 entries; AI-drafted, one of them posted with its link placeholder unfilled, a second agent credited with audits; every number treated as evidence, not resolution, until our scripts reproduce it). They answer the two R15 definitions (`P23` off the degenerate point is a bounded spectral weight, not a projector; the split term is the invariant `(lambda_2 - lambda_3)^2`), diagnose the two failed R15 predictions by Coleman's Q-ball condition (the same fact as our (iii) theorem, read at fixed J: the infimum is the delocalized `omega_c J`), correct one sentence of our R15 post (`E_h >= 0` pointwise, so `-4 I1^h` has a floor on every background and our negative hedgehog cross terms mean non-stationarity of the seed; ACCEPTED), and replace the object: v4 makes the clock an exact local symmetry of the action (conjugation by a rotation about the local director) with its own Noether charge K, a circle-averaged H-adjoint quartic, a plateau-weighted projected stiffness, and a split-weighted `E2` regulator, and pre-registers an R16 packet in our obligation-node shape (stages 0 to 4, three escapes, `mu 1e-2, c_P 1, c_s 0.4`). The scripts and report are still not linked (the third ask unanswered), so R16 runs under our stated readings of the two definitions the packet leaves open, as R15 did. R16 is two runs under one packet: R16-0, a cheap verification of every checkable claim in the four comments (symbolic, 1D, and reads on existing fields), and R16-1 to R16-4, the author's four stages on the lattice, each with its own auditor.

| Field | R16 |
| --- | --- |
| Objects | the author's v4 under OUR READINGS: `L_v4 = -4 Ibar_1^h - [V4^dd + mu rho^2] - c_P K_P^proj - c_s rho^2 E2` on the degenerate vacuum `diag(g, 1, delta, delta)` (code branch s = -1; N-spectrum `(-g, 1, delta, delta)`), g = 8, delta = 0.3, `mu = 1e-2, c_P = 1, c_s = 0.4`. Pieces: `Ibar_1^h` the circle average `(1 / 2 pi) int L(T_alpha M) d alpha` of the H-adjoint quartic, both completions named and run (`I_norm`: `F^eta` with `G = eta + 2 (eta u)(eta u)^T` in the norm, the extended registry's `I1_h`, R15's read; `I_rebuild`: `F^G = A G A - A G A`), the average taken exactly by equispaced alpha samples (the integrand is a trigonometric polynomial of bounded degree in alpha, checked by doubling the sample count); `T_alpha M = R_n(alpha / 2) M R_n(alpha / 2)^T` with `n` the eigenvector of the isolated eigenvalue 1 of N read from its Lagrange projector `P_1` (rational in N, complex-step exact); `rho^2 = (1/2) tr B^2`, `B = Pi N Pi - (1/2) tr(Pi N) Pi`, with `Pi = P23 = I - P_g - P_1` (READING 1: the packet writes `Pi` as a projector and does not say which; in the plateau domain `w(N) = P23` exactly, so the two agree there; `rho^2 = s^2 = (1/4)(lambda_2 - lambda_3)^2` on the diagonal, the R15 split term over 4); `K_P^proj` the E-density `(1/2) [sum_i tr(Om_i^T eta Om_i eta) + omega^2 tr(Om_0^T eta Om_0 eta)]`, `Om_mu = w(N) A_mu eta w(N)` with the author's PLATEAU weight (`w = 1` on `abs(lambda - delta) <= 0.5`, cosine taper to 0 at `lambda = 1` and at `lambda = -1`, zero beyond), realized label-free as the spectral function `w(N) = sum_k w(lambda_k) P_k` with its Frechet derivative by divided differences (`w[lambda_i, lambda_j]`, the limit `w'(lambda)` at coincidences), and equal to `P23` wherever the pair sits inside the plateau and the director above 0.8 (the R15 instrument re-used there, exactly); `E2 = sum_mu eta^mu tr(d_mu M G d_mu M G)` static part, the regulator entering as `c_s rho^2 E2` (E-orientation: static `+`, inertia `+ omega^2`, the author's `-c K` Lagrangian sign); `V4^dd` the certified trace-target form with the degenerate targets `C_p = (-g)^p + 1 + 2 delta^p`. The clock: the local generator `a0_loc = J_n M - M J_n` (R15's `a0_local`, which IS the author's `[G_loc, M]`), its charge K, `E_K = E_stat + K^2 / (4 kin_tot)` with `kin_tot = kin(Ibar_1^h) + c_P kin(K_P^proj) + c_s kin(rho^2 E2)`, a0 refreshed each step and frozen in the gradient (the R15 protocol) |
| Symmetry or conservation license | the author's claim, TESTED not assumed (stage 0): the local circle is an exact symmetry of `L_v4` (the potential a similarity invariant; `K_P^proj` invariant because the projected connection term `w [Theta, N] w` vanishes on the pair block, `Theta = dR R^T` having no component along `n`; `I1` NOT invariant, hence the average). Our own derivation of the first two before the run; the symmetry defect `d L_v4 / d alpha` measured on the relaxed core at 1e-10 (W0-type, the pre-registered gate); the R13-W free-inertia theorem and the author's bend theorem are theorems about the RIGID charge, so they do not decide K's minimizer question (the author's claim; R16-3 is the test) |
| Ensemble | symbolic jets (R16-0); reads on the R15 fields and 1D reduced functionals (R16-0); static relaxations on the lattice (R16-1); the fluctuation operator on the relaxed core (R16-2); fixed-K descents (R16-3); the principal symbol on the rotating core (R16-4) |
| R16-0 (the checkable claims, one auditor) | (C1) the `I_rebuild` vs `I_norm` counterexample (`A_1 = I, A_2 = E_01 + E_10` at `G = I`: `norm(F^G)^2 = 0`, `norm(F^eta)^2_G = 8`, and the reverse at `A_1 = eta`; hand-checked in-session, to be scripted) and the two forms' difference on the R15 V-b profile (the author: 2 to 6 percent; our h-column ratio 4 vs the author's 3.0 to 3.1 stays open without the bundle); (C2) the Coleman condition on the reduced line `F = int [(c / 2) s'^2 + V(s) - omega^2 c s^2]`: `V4^dd + mu s^2` has `V / s^2` minimized at `s -> 0` (our R15 theorem restated), the fixed-J infimum `omega_c J` over the volume ladder, the sextic v2 cure's crossing `omega*^2 c = mu - nu^2 / (4 kappa) = 9e-3` and plateau `s* = 0.2236` at `(1e-2, 4e-2, 0.4)`, and the weighted condition with the author's rational weight (`min U / (s^2 W) = 0.01117 > mu`, no crossing) against the plateau weight (`W = 1`, crossing restored); (C3) `E_h >= 0` pointwise for both completions (G positive definite; `tr(F G F^T G) = norm(G^(1/2) F G^(1/2))^2`) and the retraction of our floor sentence quantified: `E_h(seed)` on the dressed hedgehog against the R15 cross terms `-183 / -611 / -1696`; (C4) the local-circle action: `T_alpha` fixes every `B = 0` configuration, weight two on `B`, `T_(2 pi) = id`; the potential and `K_P^proj` invariant, `I1` not (`norm([A_1, A_2])^2 = 2 Delta^2 s^2 t^4` with an alpha-dependent cross term, the author's formula), symbolically on the sheet `M(z) = R12(psi) diag(-g, 1, delta + s, delta - s) R12^T`; (C5) the sheet inertias: `B = 0` sheets carry zero clock inertia in any action (the tangent `[J_n, B]` vanishes), the (1,2)-twisted sheet's `<F_0z, F_0z> = 8 omega^2 psi'^2 s^2 (delta + s - 1)^2` under the local generator (our R15-H `gamma = 32 omega^2 s^2 (delta + s - 1)^2` is 4x this, the `-4 I1` prefactor: the same number), nonzero at `s = 0` under the rigid generator; `K_P^proj = 0` on that sheet (R15-H4); the `rho^2 E2` stiffness `prop s^2 psi'^2` on it, and the tilt condition `c_s rho^2 kappa_2 > 16 omega^2 s^2` reducing to `c_s > 16 omega^2` (our R15-H3 with `w = c_s s^2`); the boost-sheet all-eta cross term `-8 b^2 omega^2 s^2 (g - delta -+ s)^2`; (C6) the eigenvalue kinetic metric's discontinuity `(d lambda_+)^2 + (d lambda_-)^2 = 2 (a da + b db)^2 / (a^2 + b^2)` against the smooth `rho^2 = a^2 + b^2`; (C7) the biaxial-ring reading of our R15-P-iv end state: the biaxiality field and the eigenvalue pattern on the n32 and n48 end fields (a uniaxial oblate center inside a biaxial shell, and a ring, or not), against the Landau-de Gennes biaxial-ring core of Penzenstadler and Trebin (J. Phys. France 50, 1027, 1989), the radial hedgehog as transition state (Majumdar, Eur. J. Appl. Math. 23, 61, 2012) and the sixth-order-potential torus and split cores (McLauchlan, Han, Langer, Majumdar, Physica D 459, 134019, 2024; the citations verified real, the identification is what C7 tests); (C8) `J_z = 0` on an axisymmetric configuration for any velocity field (the rotation tangent `[G_z, M] - (x d_y - y d_x) M` vanishes pointwise) and the spin-weight-2 shell decomposition of `B` (the `2Y_2m` content) built as the stage-1 diagnostic and checked on constructed patterns (`2Y_20`, real `m = +-2`, chiral `m = +2`); (C9, filler) the chiral pseudo-scalar `tau = eps_ijk M_il d_j M_kl` and `T_2 = tau^2` on our fields: the R15 hedgehog, the R3 pair, a twist sheet, a bend sheet (the author's 2e-29 / 1e-2 / 91 / 0) |
| R16-1 (stage 1, statics) | the relaxation of the R15-M n32 L48 (mu 1e-2, c_P 1) hedgehog under the STATIC `L_v4` (both quartic completions, circle-averaged), 3000 it, then n48 L72: the end texture (uniaxial radial, biaxial torus, or split core: the biaxiality field, the eigenvalue profile, the split's `2Y_2m` content on shells), the E_u and `K_P^proj` L-exponents (the author: 0), the exterior on the vacuum, and the Morse index of the radial hedgehog in the split sector (the ten-direction Hessian at the core plus the lowest modes of the split fluctuation operator): the author predicts the biaxial torus or split core at index 0 and the radial hedgehog a transition state; R15-M's uniaxial end state (max split 0.0135 under `I1` and `K_P^23`) is the prior against it |
| R16-2 (stage 2, the clock operator) | the two-component fluctuation operator in the local (2,3) block on the relaxed core (the split doublet `(a, b)` of `B`): the static stiffness from `V4^dd + mu rho^2 + c_s rho^2 E2 + c_P K_P^proj`, the inertia from `Ibar_1^h` and `c_P K_P^proj` (the author: `8 omega^2 s^2` plus the quartic's gradient part); the lowest eigenvalue of the generalized problem against `omega_c^2 = mu / c_P`: a bound doublet (`omega_0 < omega_c`, `omega_0 > 0`) or none; the Gaussian control (the author's `13/12 - sqrt(3)/18 = 0.987 < 1`) reproduced |
| R16-3 (stage 3, fixed K) | `E_K` descents on the R16-1 end field at K in {50, 200} (the R15 J = 200 kept for comparison), n32 L48 3000 it then n48 L72, with FOUR pre-registered escapes watched every 100 it: (a) a `B = 0` director sheet (the split leaves the core), (b) a split sheet with tilt, (c) a split sheet with (2,3) twist, (d) OUR ADDITION: the director leaves isolation (`lambda_1` enters the plateau taper below 0.8, the R15 seam made smooth: the plateau weight then ADDS the director to the clock block and the descent is rewarded for it); the R14-B / R15 diagnostics (stationarity by directional derivatives of the true `E_K`, `omega = K / (2 kin)`, `dE / dK = omega` along the branch, the split profile, the a0 inertia split by term); the author's prediction: none of (a) to (c) reached, a relative equilibrium, `E(K) < omega_c K` |
| R16-4 (stage 4, the principal symbol) | on the relaxed rotating core (R16-3's end state if a relative equilibrium, else the R16-1 core at the R16-2 `omega_0`): the second-order Lagrangian in every channel (tilt, split doublet, director, boost) by sympy jets on the local background, the wave-sign test per channel; the author's prediction: hyperbolic in the tilt channel for `c_s > 16 omega^2`, the exterior degenerate (no kinetic term for orientation changes where `B = 0`, a constraint structure, not a Floquet claim) |
| Admissible space | lattice fields on the boxes above, the vacuum pinned at the box edge only (shell 1.6), no taper, mask or pinned interior; the descent restricted to the spectral domain where `P_g` and `P_1` are isolated (leaving it is escape (d), a verdict, not an error); symbolic jets at a point; the reduced line for C2 |
| Representation coverage | statics, the fluctuation operator, fixed-K descents, the principal symbol; no time integrator (the 3+1 evolution of the rotating core and the Dirac constraint analysis of the exterior stay `NUMERICALLY_UNRESOLVED`, stated as such); both quartic completions run in R16-1 and R16-3, the one the author names first (`I_norm`) carrying R16-2 and R16-4 if the cores do not allow both |
| Numerical representation (frozen) | the certified stack (sym stencil, exact adjoints, FIRE dt0 0.01, dt_max 0.1) with the degenerate trace targets; the new pieces each with a complex-step gate at 1e-8 relative where holomorphic (`P_1`, `P23`, `n` from `P_1`, `rho^2`, `E2`, the plateau weight in the plateau domain) and a central-difference gate at 1e-6 elsewhere (the divided-difference Frechet derivative of `w(N)` in the taper; the circle average's chain rule through `n(M)`); the identities `w(N) = P23` in the plateau domain, `w^2 != w` in the taper (a weight, not a projector), `T_(2 pi) = id`, `rho^2 = s^2` on the diagonal, the alpha-sample doubling test at 1e-12; covariance under random boosts and rotations with a no-eta control failing; the same `killswitch`, per-rung notification, resume ping and watchdog discipline as R15 |
| Permitted verdicts | R16-0: CONFIRMED / QUALIFIED / REFUTED per claim C1 to C9. R16-1: `UNIAXIAL_RADIAL` / `BIAXIAL_TORUS` / `SPLIT_CORE` with the Morse index of the radial hedgehog. R16-2: `BOUND_DOUBLET` (`0 < omega_0 < omega_c`) / `NO_BOUND_MODE`. R16-3: `PERIODIC_ORBIT_EXISTS` (the R14-B convergence gates, `dE / dK = omega`) or `CANDIDATE_REFUTED` with the escape named, (a) to (d). R16-4: `HYPERBOLIC` / `NOT_HYPERBOLIC` per channel with the exterior's degeneracy stated |
| Failure scope | an R16-3 refutation is of `L_v4` at `(mu, c_P, c_s) = (1e-2, 1, 0.4)` under our two readings (`Pi = P23`; the director from `P_1`), with the named quartic completion; escape (d) reached refutes the plateau weight as a seam cure and asks the author for the director's definition past isolation; R16-1 landing on the uniaxial radial hedgehog at Morse index 0 refutes the biaxial-torus prediction on this potential, not the Landau-de Gennes result it was read from; R16-0 refutations correct the author's text, not the object |
| Fallbacks (the ladder never waits) | the circle-averaged gradient too slow for 3000 it: 1000 it on n32 with the n48 rung dropped and stated; the Frechet derivative in the taper failing its gate after three tries: the run confined to the plateau domain with (d) declared on entry; no bound doublet in R16-2: R16-3 runs anyway (the author's object predicts the doublet, the descent is the independent test) and R16-4 uses the R16-1 core at `omega_c / 2`; R16-1 not localized: the R15-M field carries R16-2 and R16-3 with the caveat; an instrument defect: fixed as a logged deviation, the rung re-run, three tries then STOP on that gate |
| Analytic closure carried in | R15's projector identities and H-adjoint equality (they transfer to `w(N)` on the plateau domain, where it IS `P23`); the R15 (iii) theorem (Coleman's condition, the author's reading adopted); the R13-W W0 theorems and the author's bend theorem (rigid charge only); R14-0's Goldstone count; the C4 invariance derivation (this packet) |
| Unlocks | R16-3 a periodic orbit: the G1 to G7 battery on `L_v4` and the 3+1 integrator question posted; escape (d): the author's third definition; R16-1 a biaxial torus: the R12 ring record re-read against it; R16-0 C7 confirmed: the P-iv end state named in the record as the biaxial-ring core; the bundle arriving at any point: a `REPRODUCED` column added to every row it covers, nothing re-run on its say-so |
| Run shape | R16-0 first, in the foreground on the day (about 2 to 3 h: sympy for C1, C4 to C6, 1D for C2, reads on the R15 fields for C3, C7, C9, the `2Y_2m` builder for C8), its auditor steering nothing (the claims are the author's); then the instrument build for R16-1 to R16-4 (the circle-averaged quartic with both completions and gradients, the plateau weight with its Frechet derivative, `rho^2 E2`, the symmetry-defect gate, the fluctuation operator; about 4 to 6 h with gates) and the four stages as ONE autonomous overnight session on the 16-core machine (R16-1 relaxations detached on 8 cores as soon as the instrument passes; R16-2 on its end field; R16-3 the long pole, 5 to 8 h on 8 cores; R16-4 symbolic in the foreground); one independent auditor per stage before its verdict steers the next; the results post on #186 in the TL;DR shape, opening with the retraction of our floor sentence and the naming of our completion, with escape (d)'s definition question and the fourth scripts ask in the body |

**Two things stated before the go.** First, the author's packet stage 0 asks for a "canonical-action audit" of signs and generator normalization: on our stack every term is carried in E-orientation (static `+`, inertia `+ omega^2`), which IS the author's `-c K` Lagrangian convention, so the sign correction changes nothing in our numbers and stage 0 reduces to the symmetry-defect gate and the spectral-domain statement (folded into R16-0 and the instrument gates). Second, the four comments also list directions R16 does not run (the bend theorem beyond C9, the Q-hopfion, the signed charge, the propagating twist mode, the Einstein-aether columns, the gauge sector, the atomic gates, the Longa-Trebin LP, V1 on the M5.8 signed quartic): parked in § 7 until R16 decides whether v4 has a rotating core at all.

**Outcome of R16-0 (2026-09-06, the verification rung ran 12:42 to 13:38 UTC; the [task record](../tasks/m5_32_task_details.md) R16-0 section for every number and the audit).** Audited 13 / 4 / 0 (17 claims). C1: the counterexample exact; on the witness jet the two completions coincide, on the witness lattice they differ by 24 to 46 percent (not 2 to 6), and the author's posted h column is `I_rebuild / 4` while ours is `I_norm` (the R15 3.0-vs-4 mismatch closed: the completion, not the pair ordering). C2: Coleman's diagnosis reproduced with the numbers (the sextic crossing at 0.0090, `s* = 0.2236`); two normalizations named (`omega_c^2 = mu / (4 c_P)` for `U = mu rho^2`; the author's 0.01117 is the value at `s*`, the minimum is `mu`); the sextic's Q-ball needs J above 2.6e4 and a radius above 69, outside every lattice box of the record. C3: `E_h >= 0` pointwise for both completions (our floor sentence retracted); `I_rebuild` keeps the sign flip on the relaxed hedgehog (`+195 / +629 / +1500`), `I_norm` does not. C4: the local circle is exact on the potential and `K_P^23` (jets exact, lattice `O(h^2)`), not on `I1`, not on either completion, and NOT on the regulator `E2` either: `L_v4` as written is not circle-invariant, the average must cover `E2` (the R16 instrument will). C5, C6: every sheet formula exact (the boost-sheet law reads `(g + delta +- s)^2`). C7: the P-iv end state has an oblate uniaxial center (the R15 crossing) and a `beta^2 = 1` ring of radius 2.9 with the great-circle quadrupole signature, axis on the lattice body diagonal, identical in cell units on both boxes: the Landau-de Gennes topology at lattice scale, refinement owed. C8: `J_z = 0` on axisymmetric configurations confirmed (`O(h^2)`), the `2Y_2m` diagnostic built and gated, the P-iv split achiral. C9: the pattern 0 / small / large / 0 reproduced. Net: object v4 survives R16-0 with one definitional repair (the averaged regulator) and two normalization notes; the second go builds the instrument.

**R16-0 amendments to the packet (2026-09-06, approved; the run order R16-1 to R16-4 and the run shape unchanged).** (1) The object: BOTH the quartic and the regulator circle-averaged, `L_v4 = -4 Ibar_1^h - [V4^dd + mu rho^2] - c_P K_P^proj - c_s bar(rho^2 E2)`, the average taken exactly by two equispaced samples per term (every density is a trigonometric polynomial of degree one in alpha, R16-0 C4b; the doubling test kept as the gate); the symmetry-defect gate (`d L_v4 / d alpha` at 1e-10 on the relaxed core) applies to the FULL action, and an unaveraged `rho^2 E2` is the pre-registered control that must FAIL it. (2) The completions: `I_norm` and `I_rebuild` both, named on every number; `I_rebuild` is the author's (R16-0 C1, C3), so it carries R16-2 and R16-4 if the cores do not allow both (the reverse of the earlier default). (3) The gap: `omega_c^2 = mu / (4 c_P) = 2.5e-3` (`omega_c = 0.05`) for `U = mu rho^2`, the R16-2 bound-doublet reference; the fixed-K rungs at K in {50, 200} stay, with the R16-0 scale statement on record (no reduced-line Q-ball below J 2.6e4, so a localized R16-3 state, if any, is the core-bound doublet, not a Q-ball). (4) R16-1 adds the h-refinement: the n64 L48 relaxation (h 0.75, 8 cores, fallback: dropped and stated) so the biaxial ring's radius and axis are read in physical units, and the spin-2 shell content and the biaxiality map on every end field (R16-0 C7, C8 instruments). (5) Escape (d) unchanged; the director from `P_1`, the plateau weight `w(N)` as the spectral function with its divided-difference derivative. (6) The results post opens with the retraction of the floor sentence, the naming of the completions, the two normalization items and the regulator repair, then the stage verdicts; the fourth scripts ask in the body.

**Outcome of R16 (2026-09-06 13:54 to 2026-09-07 01:45 UTC, the instrument and the four stages; the [task record](../tasks/m5_32_task_details.md) R16 section, the [method note § 14](m5_32_method_note.md), one independent auditor per stage).** The instrument realized v4 under the two readings with every term circle-averaged (43 gates; 8 samples exact on the lattice, where the density has degree 4 in alpha, 4 in the descents). R16-1: `UNIAXIAL_RADIAL` on n32 L48, n48 L72 and n64 L48 (h 0.75), the seed's split shrinking tenfold, the exterior `r^-4`, the radial hedgehog at Morse index 0 in the split sector (audit 4/3/0). R16-2: `NO_BOUND_MODE` on the seed and the relaxed core (the cores' lowest doublet modes 0.0448 above the empty box's bottom 0.0256 and above `mu / c_P` 0.01; the first frequencies were 2x, caught by the audit 1/2/3, corrected and re-run). R16-3: `CANDIDATE_REFUTED` (d, b) at n32 K 50 and `NUMERICALLY_UNRESOLVED` on the four other runs: every descent builds a lattice-scale split spike 3.5x to 6.6x above `omega_c K`, never a relative equilibrium, the inertia the quartic's (audit 4/3/0, with the finding that R15's frozen-a0 protocol is not a descent of the true `E_K` at these states: the stage-3 verdicts are provisional until re-run with the true gradient). R16-4: `HYPERBOLIC` in every channel on the cores at every omega to 0.25; the spikes flip `omega`-driven by the kinetic quartic's `32 omega^2 rho^2`, the author's `c_s > 16 omega^2` generalized (audit 2/2/1, correcting the producer's static attribution). Twelve deviations logged (the sample count, escape (d) redefined as loss of isolation, the completions' identity on unboosted fields, the label-free weight off the plateau, the n64 seeds, the lift, the factor 2). Net: v4 at `(1e-2, 1, 0.4)` has no bound doublet on the relaxed core and no relative equilibrium at fixed K on the lattice, and the statics go uniaxial; the next instrument fix is the true fixed-K gradient; the results post (the TL;DR shape, the method-note body with the plots inline) is drafted for the user's decision.

## 7. Not computed here

| Item | Why not |
| --- | --- |
| Any re-derivation of the P239 to P248 numbers (the six items queued in the Vantasner digest § 4: the 0032 quadratic no-go, the P239 H kernel, the 0034 Legendre signs, the P243 census, the compactness chain, the P247 G1 Hessian) | this rung is a ledger, not a verification campaign; each is a candidate audit item and is listed so the next rung can pick it up; the P240 0034 two-clock algebra was already verified symbolically by Mikulski 003 |
| The Mikulski 004 GPU fields (and everything downstream of them: the 006 lattice-cost figure, the 007 lattice producers, the 008 well itself) | 004's 45-minute GPU pipeline was not run on this machine; the committed fields' energies were re-evaluated by the author's numpy route 2 here (1.8e-15), which checks the record, not the relaxation |
| The P249 variational bridge (Benci-Fortunato Theorem 18 hypotheses EC-0 to EC-3, translation splitting, strict hylomorphy) | unchanged since 2026-08-31: their review record carries it; our check covers the exact algebra only |
| Box independence of any Mikulski clock | never measured by its author; not ours to run before the ledger is posted |
| A dynamical (time-integrated) test of wall formation | no Hamiltonian integrator exists for `L_cert` in this stack; see § 6.2 representation coverage |

## 8. Adversarial audit record

An independent agent (own scripts, own reading of the pinned sources, the digests treated as evidence only) audited every status cell, both repair tables, the ranking, and the R13-W packet, on 2026-09-02. Report: `scratchpad/r13l/audit.md` (local); scripts `audit_wall_commutator.py`, `audit_bag_energetics.py`, `audit_two_clock_legendre.py`, `audit_six_invariants.py`, `audit_format.py`, plus a rerun of `m5_32_p249_check.py` (7/7).

| Claims checked | CONFIRMED | QUALIFIED | REFUTED |
| --- | --- | --- | --- |
| 70 (§ 2: 11, § 3: 9, § 4: 9, § 5.1: 11, § 5.2: 13, § 6.1: 6, ranking claims: 2, scripts: 5, formatting: 4) | 53 | 16 | 1 |

| Finding | Applied |
| --- | --- |
| REFUTED: the degenerate-vacuum row said "nobody / untested"; the OpenWave record (R7 N3, R8 S6, R9 B3/B4: rigid inertia exactly zero at `delta = 0`, escape CLOSED) and the P249 route history (commuting split collapses on the degenerate P239 exterior) had tested it | § 5.2 row rewritten: closed on the rigid family, open only as a relaxed fixed-J object; § 6.2 unlocks updated |
| QUALIFIED: C-M5S-008 is the confined two-clock pair of the spectral-Cartan action (P240 0043), not a raw-M5 or boost-dressed pair | § 3 header and P243 row corrected |
| QUALIFIED: the P247 quote "refuted in the extended class" is not in the pinned repo; "exact Z2 evenness" is machine precision on measured backgrounds; the energy ladder is two segments at orders 16 and 24, non-monotone at the junction | P247 row rewritten with C-M5S-015's own sentence |
| QUALIFIED: Mikulski 006 `t_1` clean ceiling is 1.380 (1.427 is the cutoff-flagged row) | § 4 row corrected |
| QUALIFIED: Mikulski 009's `L^2.93` is a volume law of a different mechanism, not the OpenWave `1/r`-tail mechanism | § 5.2 split into two rows |
| QUALIFIED: § 6.1 "wall-pinned clocks closed" contradicted § 5.2 and C-M5S-015's exclusions | closed row now says "on the tested ladders" |
| QUALIFIED: rank 1 is a different field theory; rank 3 is not on the certified action; rank 4's "ensemble artifact" inference conflated the static pair with the clock-clock term; rank 5 had a named OpenWave term (R11 U2) | all four cells rewritten |
| QUALIFIED: the bag closure mixed `J^2/(2 kin)` with the document's `J^2/(4 kin)` and assumed `kin ~ R^3` against the measured `kin ~ L` | rewritten with both laws, own convention |
| QUALIFIED: W2's `sigma(Delta omega)` was not a static observable; the interface energy is periodic in the relative angle and the wall carries an `omega^2` inertia | W2 redefined as `E_stat(Delta q)` over one period plus `kin_wall`; license row carries the derivative condition |
| QUALIFIED: the wall row's "nobody" ignored two analogs (Mikulski 009 § 5 masked-generator interface term; 009 § 6a / 011 core-confined rotation); the 2026-08-28 quotation was not on any pinned record | both rows rewritten; quotation removed, message cited by date |
| QUALIFIED: routes missing from § 5.1 (Yukawa boost exchange, frozen telescope, literal negative-square metric, gaussian pocket, `xi`/`B` escape) and § 5.2 (P239 F to K, P240 L1/L2/Skyrme/sextic, commuting split, core-breaking texture, `Pgrad`/`J1`/`J2`, rigid clock at `delta = 0`) | eleven rows added |
| QUALIFIED: vacuum written three ways; the reports' `I_1` is twice our `I1` | notation unified to `diag(-g, 1, delta, 0)`; convention note in § 4 |
| CONFIRMED by script: `a0 = [G1, M] = (d2 - d3)(E23 + E32)`; `F_0i = 0` on a uniform vacuum; the two-clock Legendre signs; the six-invariant witness `(4,4,2,2,2,4)` and static rank 3; P249 7/7 | cited in the rows |
| Formatting: no dashes, allowed emojis only, 11 tables consistent, 16 relative links resolve | clean |
