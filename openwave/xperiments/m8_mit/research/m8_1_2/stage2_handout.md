# Stage 2: two further items

Same conventions and definitions as the stage-1 file; nothing there changes. Answer both, in the same return format.

**19 (continued).** For a number that can be compared, fix the normalisation explicitly: put
$`L_8(u) = \left[\, \rho_6(u) \otimes u \,\right]_8`$, using the couplings and the
Clebsch-Gordan phase fixed above, and report the $`N = 3`$ component $`\left(L_8(v_3)\right)_3`$
as an exact value.

**20.** Item 12 builds $`R_K = \mathcal{M}_K(P)`$. Read $`R_6(P)`$ as a binary form of degree 12
in the Majorana correspondence fixed in the conventions block above, and report its root
multiset. **State whether the roots are simple or repeated, and give the multiplicity of each**,
rather than reporting positions alone. Then determine whether
the map sending a state $`v \in V_3`$ to the first transvectant of that degree-12 form with the
sextic $`F_v`$ is injective, and say what the root multiset has to do with the answer. If you
establish injectivity by a numerical rank computation rather than from the multiplicities, say so
explicitly; the two are different evidence.
